---
phase: 06-aufr-umen
plan: 07
type: execute
wave: 5
depends_on: ["06-04", "06-06"]
files_modified:
  - internal/plugin/wasmtest/wasmtest.go
  - internal/plugin/runtime_test.go
  - internal/plugin/sdk_e2e_test.go
  - internal/plugin/hofladen_e2e_test.go
  - internal/public/formular_e2e_test.go
  - internal/public/suche_e2e_test.go
  - .github/workflows/ci.yml
  - .github/workflows/security.yml
  - .github/workflows/release.yml
  - .planning/codebase/TESTING.md
  - .planning/codebase/STRUCTURE.md
  - docs/offene-punkte.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [MAINT-04, MAINT-05]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "A test that cannot find its guest module fails on a runner and skips on a contributor's machine, and the difference is one named environment variable rather than a guess about the host."
    - "The failure message names `go run ./tools/wasm` as the command that builds the missing file, in one place, for all five call sites."
    - "The three workflows that run tests set `HOLZCLOUD_TEST_REQUIRE_WASM`; the one that runs none does not."
    - "The promotion landed after the rebuild-and-compare, not before."
    - "`TESTING.md`, `STRUCTURE.md` and `docs/offene-punkte.md` describe the CI, the tool inventory and the standing instructions this phase leaves behind."
    - "The ROADMAP planning note that pointed at the indentation claim in `tools/i18n/main.go` no longer points at something that does not exist — the phase does not end by re-staling the very note it repaired."
    - "`go run ./tools/i18n` reports zero open and zero orphaned, and the rebuilt guest modules were driven once through the running application in a browser."
  artifacts:
    - internal/plugin/wasmtest/wasmtest.go
    - .github/workflows/ci.yml
    - .github/workflows/security.yml
    - .github/workflows/release.yml
    - .planning/codebase/TESTING.md
    - .planning/codebase/STRUCTURE.md
    - docs/offene-punkte.md
    - .planning/ROADMAP.md
  key_links:
    - "`06-03`'s deletion of the indentation claim → the ROADMAP planning note that pointed at it. This plan is the only place in the phase where that note is re-derived after `tools/i18n/main.go` reaches its final shape."
    - "`internal/plugin/wasmtest` → three distinct Go packages (`plugin`, `plugin_test`, `public`); a helper declared in a test file cannot reach all five call sites, which is why this is a non-test package."
    - "workflow-level `env:` in `ci.yml`, `security.yml` and `release.yml` → the helper's decision; `image.yml` runs no tests and is deliberately untouched."
    - "the four rebuilt plugin guests → the public pages that render them; the browser pass is what proves a recompiled guest still works in the running application."
---

<objective>
Promote the five self-skipping tests, now that CI rebuilds what they read, and
close the phase's own loop: the documents that describe the CI this phase changed,
and the standing gate.

Purpose: MAINT-04 and the remainder of MAINT-05. A test that skips itself reports
green and checks nothing. It stays forgiving on a contributor's machine — a partial
checkout is not a defect — and becomes a failure on a runner, where a missing guest
module means something is wrong. D-12's ordering is satisfied by construction:
plan 06-06 already landed the rebuild and the comparison, so a red suite here is
always fixable with one command.

Output: a shared helper serving three packages, five rewritten call sites, one
environment variable in three workflows, three corrected documents, and the
standing gate run and recorded.
</objective>

<execution_context>
@/Users/holz/.claude/gsd-core/workflows/execute-plan.md
@/Users/holz/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-aufr-umen/06-CONTEXT.md
@.planning/phases/06-aufr-umen/06-RESEARCH.md
@.planning/phases/06-aufr-umen/06-PATTERNS.md
@.planning/phases/06-aufr-umen/06-06-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One shared helper decides skip-or-fail for all five call sites (MAINT-04, D-11)</name>
  <precondition>Plan 06-06 landed: `go run ./tools/wasm -check` exits 0 against the committed tree and `ci.yml` runs it before `go test ./...`. Promoting the skips before that would hand a contributor a red suite they cannot fix.</precondition>
  <read_first>
    - internal/plugin/runtime_test.go — lines 13–29, the helper being extracted and its German doc comment
    - internal/plugin/sdk_e2e_test.go — lines 1, 19–26
    - internal/plugin/hofladen_e2e_test.go — lines 1, 21–28
    - internal/public/formular_e2e_test.go — lines 1, 156–163
    - internal/public/suche_e2e_test.go — lines 1, 24–31
    - .planning/phases/06-aufr-umen/06-RESEARCH.md — §"Pattern 3: A shared skip-or-fail helper across three test packages" (the package table and why a test-file helper cannot serve all five), §"The shared wasm helper (MAINT-04, D-10, D-11)"
    - .planning/phases/06-aufr-umen/06-PATTERNS.md — §"`internal/plugin/wasmtest/wasmtest.go` (NEW — test helper package)", including the five call sites as they read today
    - .planning/phases/06-aufr-umen/06-CONTEXT.md — D-09, D-10, D-11
  </read_first>
  <files>internal/plugin/wasmtest/wasmtest.go, internal/plugin/runtime_test.go, internal/plugin/sdk_e2e_test.go, internal/plugin/hofladen_e2e_test.go, internal/public/formular_e2e_test.go, internal/public/suche_e2e_test.go</files>
  <behavior>
    - With the guest module present, the helper returns its bytes and the test proceeds — unchanged from today, in both environments.
    - With the module absent and `HOLZCLOUD_TEST_REQUIRE_WASM` unset, the test skips and the skip message names the module path and the build command.
    - With the module absent and `HOLZCLOUD_TEST_REQUIRE_WASM` set to any non-empty value, the test fails and the failure message names the module path, the underlying read error and the build command.
    - Setting the variable to `true` or `yes` behaves the same as setting it to `1` — a contributor reproducing a CI failure must not silently keep skipping because of a strict value comparison.
    - All five call sites produce the same message shape, because there is one message.
  </behavior>
  <action>
Create `internal/plugin/wasmtest/wasmtest.go`, a non-test package importing only
`os` and `testing`. Nothing in production imports it, so it adds nothing to the
shipped binary — the same shape the standard library uses for its own test helpers.
It has to be a non-test package because the five call sites live in **three**
different Go packages, and a helper declared in a test file under one of them
cannot be seen by the other two. Duplicating it would put D-11's message in two
places, and MAINT-04 is precisely a requirement about one consistent message.

**Comments in `internal/` are German** — this is `internal/`, unlike `tools/`.
Give the package a doc comment several lines long in the register of the helper it
replaces: on a contributor's machine a missing guest module is not an error,
because they may have checked out only part of the tree; on a runner it is one,
because a test that skips itself reports green and checks nothing; the environment
variable is what tells the two apart, and the three workflows that run tests set it.

Export one function taking a `*testing.T` and a module path relative to the test's
own directory, returning the module bytes. First line is `t.Helper()`. It reads the
file; on success it returns the bytes. On a read error it decides: if
`HOLZCLOUD_TEST_REQUIRE_WASM` holds any non-empty value it fails the test, and
otherwise it skips. Compare against emptiness, not against the literal `1` — D-10
sets the value to `1`, but somebody reproducing a CI failure locally will reach for
`true` or `yes` and a strict comparison would silently keep skipping, which is the
exact failure mode this requirement exists to remove. Both branches carry the same
German message body: the path, the underlying error, and on its own line the
instruction that the file is built with `go run ./tools/wasm`. Declare that
instruction once as a package-level constant so the two branches cannot drift.

Then rewrite the five call sites, each currently a four-line read-and-skip block,
into a single call to the helper. Keep each variable name as it is so the rest of
each test is untouched:

- `internal/plugin/runtime_test.go` — the private `echoModul` helper becomes a
  one-line wrapper delegating to the shared function with `testdata/echo.wasm`.
  Keep its existing German doc comment about why the module is committed; it is
  still true and still the right place for it.
- `internal/plugin/sdk_e2e_test.go` — `../../plugins/jahreszahl/plugin.wasm`
- `internal/plugin/hofladen_e2e_test.go` — `../../plugins/bestellung/plugin.wasm`
- `internal/public/formular_e2e_test.go` — `../../plugins/kontaktformular/plugin.wasm`
- `internal/public/suche_e2e_test.go` — `../../plugins/suche/plugin.wasm`

The tests keep reading the **committed** file (D-09). Because plan 06-06 proved the
committed file equals a fresh build, no rewiring is needed and MAINT-04 stays
exactly as scoped. Leave the neighbouring manifest reads alone — they are not part
of this change.

Prove both behaviours before finishing, in a temporary directory or with a file
moved aside and restored: with the module absent and the variable unset, the test
skips; with the module absent and the variable set, it fails and the message names
the build command. Record both outputs in the SUMMARY. A helper whose failing
branch was never executed is not a promotion.
  </action>
  <acceptance_criteria>
    <!-- planner-discipline-allow: t.Skipf -->
    <!-- planner-discipline-allow: os.ReadFile -->
    - `go build ./... && go vet ./...` exits 0 and `gofmt -l .` prints nothing.
    - `go test ./internal/plugin/ ./internal/public/` exits 0 with the modules present.
    - `grep -rn 't.Skipf' internal/plugin/*_test.go internal/public/*_test.go | grep -c 'plugin.wasm\|echo.wasm'` reports 0 — no call site decides for itself any more.
    - `grep -rn 'os.ReadFile' internal/plugin/*_test.go internal/public/*_test.go | grep -c '\.wasm'` reports 0.
    - All five files import `internal/plugin/wasmtest`, verified with `grep -l wasmtest`.
    - `internal/plugin/wasmtest/wasmtest.go` imports exactly `os` and `testing` and nothing else.
    - With `plugins/suche/plugin.wasm` moved aside and the variable unset, `go test ./internal/public/ -run Suche -v` reports a skip whose message contains the path and `go run ./tools/wasm`.
    - With the same file moved aside and `HOLZCLOUD_TEST_REQUIRE_WASM=1`, the same command exits non-zero and the failure message contains the path and `go run ./tools/wasm`.
    - With `HOLZCLOUD_TEST_REQUIRE_WASM=true` the behaviour is identical to `=1`.
    - The file is restored and `go test ./...` exits 0 at the end of the task.
  </acceptance_criteria>
  <verify>
    <automated>go build ./... &amp;&amp; go vet ./... &amp;&amp; gofmt -l . &amp;&amp; go test ./internal/plugin/ ./internal/public/</automated>
    <fails_when>non-zero exit, `gofmt -l` naming any file, or a `--- FAIL` line</fails_when>
    <automated>mv plugins/suche/plugin.wasm /tmp/suche.wasm.bak; go test ./internal/public/ -run Suche -v 2>&amp;1 | tail -20; echo "exit=$?"; HOLZCLOUD_TEST_REQUIRE_WASM=1 go test ./internal/public/ -run Suche 2>&amp;1 | tail -10; echo "required_exit=$?"; mv /tmp/suche.wasm.bak plugins/suche/plugin.wasm</automated>
    <fails_when>the first run does not print a SKIP line naming `go run ./tools/wasm`, or `required_exit=0` — the promoted branch did not fire</fails_when>
    <automated>grep -rn 't.Skipf' internal/plugin/*_test.go internal/public/*_test.go | grep -c '\.wasm'</automated>
    <fails_when>a count other than 0 — at least one call site still decides for itself and the message is no longer in one place</fails_when>
    <automated>go run ./tools/wasm -check &amp;&amp; go test ./...</automated>
    <fails_when>non-zero exit — the tree must be whole and green when the task ends</fails_when>
  </verify>
  <done>One function in one package decides skip-or-fail for five tests across three packages, both of its branches were executed and recorded, and the failure names the command that fixes it.</done>
</task>

<task type="auto">
  <name>Task 2: Set `HOLZCLOUD_TEST_REQUIRE_WASM` repo-wide in the three workflows that run tests (D-10)</name>
  <read_first>
    - .github/workflows/ci.yml — lines 15–17, the workflow-level `env:` block with its German comment
    - .github/workflows/security.yml — lines 11–12, the same block without a comment, and lines 34–47, the race-detector step whose comment carries the 297 s measurement and which shows the per-step `env:` override form
    - .github/workflows/release.yml — lines 12–14, the same block with the same comment
    - .github/workflows/image.yml — enough to confirm it runs no tests and is correctly left out
    - .planning/phases/06-aufr-umen/06-PATTERNS.md — §"Workflow-level `env:` in three workflows (D-10)"
    - .planning/phases/06-aufr-umen/06-CONTEXT.md — D-10
  </read_first>
  <files>.github/workflows/ci.yml, .github/workflows/security.yml, .github/workflows/release.yml</files>
  <action>
Add the variable beside the existing pure-Go-driver variable in the workflow-level
`env:` block of the three workflows that actually run tests: the CI workflow, the
weekly security workflow, and the release workflow. Value `"1"`, quoted, matching
the quoting of the variable it sits beside. Add a German `#` comment above it in
the same register: a skipped test reports green, so on a runner a missing guest
module is an error and on a development machine it is not.

The container image workflow runs no tests and stays untouched. That is deliberate,
not an omission — say so in the SUMMARY so a later reader does not "fix" it.

Anything on a GitHub runner is CI, which is why the security workflow is included
even though it runs on a schedule. Exempting it would leave the hole open exactly
where the plugin tests run most thoroughly: it is the one place the race detector
runs, measured at 297 s for the plugin package alone.

Change nothing else in any of the three files. Add no step, no action, and no
per-step override. Confirm afterwards that each file still parses and that the
`env:` block sits at workflow level, not inside a job.
  </action>
  <acceptance_criteria>
    - `grep -c 'HOLZCLOUD_TEST_REQUIRE_WASM' .github/workflows/ci.yml .github/workflows/security.yml .github/workflows/release.yml` reports 1 for each.
    - `grep -c 'HOLZCLOUD_TEST_REQUIRE_WASM' .github/workflows/image.yml` reports 0.
    - In each of the three files the variable is in the top-level `env:` mapping, not inside a job or a step — verify by parsing the YAML.
    - The value is the string `1`.
    - A German `#` comment sits immediately above the variable in each of the three files.
    - `grep -c 'uses:' ` reports the same count as before this task for each of the three files.
    - All four workflow files parse as YAML.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
import yaml
for f in ['ci','security','release']:
    d=yaml.safe_load(open('.github/workflows/%s.yml'%f))
    assert d['env']['HOLZCLOUD_TEST_REQUIRE_WASM']=='1', f
d=yaml.safe_load(open('.github/workflows/image.yml'))
assert 'HOLZCLOUD_TEST_REQUIRE_WASM' not in (d.get('env') or {}), 'image.yml must stay untouched'
print('ok')"</automated>
    <fails_when>non-zero exit, an `AssertionError`, or output other than `ok` — the variable is missing, at the wrong level, has the wrong value, or leaked into the image workflow</fails_when>
    <automated>grep -c 'HOLZCLOUD_TEST_REQUIRE_WASM' .github/workflows/image.yml</automated>
    <fails_when>a count other than 0</fails_when>
  </verify>
  <done>Every workflow that runs a test declares the variable that turns a self-skipping test into a failing one; the one workflow that runs no tests declares nothing.</done>
</task>

<task type="auto">
  <name>Task 3: Close the documentation loop on every fact this phase itself created or invalidated (MAINT-05)</name>
  <read_first>
    - .planning/codebase/TESTING.md — lines 25–26, the two lines describing the CI and security workflows, deliberately left alone by plan 06-04
    - .planning/codebase/STRUCTURE.md — line 21, the `tools/` inventory line, deliberately left alone by plan 06-04
    - docs/offene-punkte.md — the `## Beim Weiterarbeiten` section as plan 06-04 left it
    - .github/workflows/ci.yml — the finished `test` job, so the description matches it step for step
    - tools/wasm/main.go — the pin constant and its comment, so the standing instruction quotes the real constraint
    - .planning/phases/06-aufr-umen/06-RESEARCH.md — §"Pitfall 3" (the pin may not fall below the root module's `go` directive) and the note under §"H. `docs/offene-punkte.md`" prescribing this bullet
    - .planning/ROADMAP.md — the Phase 6 planning note whose sub-clause (a) points at the indentation claim in `tools/i18n/main.go`, as plan `06-01` left it
    - tools/i18n/main.go — the `writeCatalog` doc comment in its final shape, as plan `06-03` left it
    - .planning/phases/06-aufr-umen/06-CONTEXT.md — the **WITHDRAWN** D-17a block, so the retirement wording does not resurrect a correction that was already reversed
  </read_first>
  <files>.planning/codebase/TESTING.md, .planning/codebase/STRUCTURE.md, docs/offene-punkte.md, .planning/ROADMAP.md</files>
  <action>
Plan 06-04 corrected everything that was already stale and deliberately left three
anchors alone, because this phase was about to change what they describe. A fourth
anchor was invalidated by the phase itself. Correct all four now, against the
finished tree — this is the last chance the phase has to avoid ending on the same
defect class it opened by removing.

`.planning/codebase/TESTING.md`, the two lines describing the workflows. Rewrite the
CI line so it lists the `test` job's steps in the order they now run, including the
module comparison and the catalogue check, and mention that the workflow sets the
variable that turns a missing guest module into a failure. Rewrite or extend the
security line so it also notes the variable. Keep the sentence shape and the
register of the surrounding file; these are two lines, not a section. Also add the
new test command to the run-commands block if the block would otherwise not mention
the guest-module tooling — a reader looking for how to run the suite should find
the command that produces what the suite reads.

`.planning/codebase/STRUCTURE.md`, the `tools/` inventory line. It names two helpers
in its parenthetical; there are now three. Add the new one with a two-or-three-word
description matching the register of the other two.

`docs/offene-punkte.md`, `## Beim Weiterarbeiten`. Add one bullet in the shape of
the ones already there — a bolded lead-in, then the instruction. It says: the guest
modules are built by `go run ./tools/wasm`, which pins the compiler; the pin lives
as a constant in that file and may not fall below the `go` directive in the root
`go.mod`, because the test guest lives in the root module and the go command refuses
to load a module demanding a newer version than the running toolchain; so raising
the Go version means raising the pin and rebuilding the guests, and the rebuild
belongs in its own commit containing nothing but the build artifacts. Without this
bullet the constraint is discovered by whoever merges the next Go bump, when all six
targets turn red at once.

`.planning/ROADMAP.md`, the Phase 6 planning note whose sub-clause (a) says the
`tools/i18n` doc comment "still" claims indented output and points at a line number.
Plan `06-03` deleted that claim and, in the same edit, removed the per-string helper
above it — so the sentence is false and the line number points at whatever moved
into its place. Confirm both before rewriting:
`grep -n 'sorted and indented' tools/i18n/main.go` must now print nothing.

**Retire the sub-clause rather than re-pointing it.** A pointer to a claim that no
longer exists has nothing to point at, and a fresh line number into a file this
phase just reshaped would only start drifting again. Rewrite it to record the
finding as closed: the doc comment on `writeCatalog` now describes the output as
sorted and flush left, one key per line; the package doc comment never made an
indentation claim; and the format is held by the round-trip test in
`tools/i18n/main_test.go` rather than by a note in the roadmap. Keep sub-clause (b)
about `fr-CH.json` and `it-CH.json` — also closed by `06-03`, and stamp it as such
in the same register. Change nothing in the note about wasm, the pin or
`-buildvcs=false`; those describe work that is now real. Touch no other phase.

Every claim in these four edits must be checkable against the tree at the moment
it is written. Record the checking command beside each in the SUMMARY, the same way
plan 06-04 did.
  </action>
  <acceptance_criteria>
    - `.planning/codebase/TESTING.md`'s CI line names the module-comparison step and the catalogue step, and its step order matches the order in `.github/workflows/ci.yml`.
    - `.planning/codebase/TESTING.md` contains the substring `HOLZCLOUD_TEST_REQUIRE_WASM`.
    - `.planning/codebase/STRUCTURE.md`'s `tools/` line names all three helpers under `tools/`; `ls -d tools/*/ | wc -l` reports 3.
    - `docs/offene-punkte.md`'s `## Beim Weiterarbeiten` section contains the substrings `tools/wasm`, `GOTOOLCHAIN` (or the German phrasing naming the pin) and `go.mod`.
    <!-- planner-discipline-allow: sorted and indented -->
    - The new bullet states that the rebuild belongs in its own commit.
    - `grep -n 'sorted and indented' tools/i18n/main.go` prints nothing, and no line inside `### Phase 6` of `.planning/ROADMAP.md` still asserts that the doc comment claims indented output.
    - The Phase 6 planning note carries no line reference into `tools/i18n/main.go`: `sed -n '/### Phase 6/,/### Phase 7/p' .planning/ROADMAP.md | grep -c 'main\.go:[0-9]'` reports 0.
    - `sed -n '/### Phase 6/,/### Phase 7/p' .planning/ROADMAP.md | grep -c 'tools/wasm'` still reports at least 1 — the wasm notes plan `06-01` added were not collateral damage.
    - `git diff -- .planning/ROADMAP.md` shows no change to any line inside `### Phase 7`, `### Phase 8`, `### Phase 9` or `### Phase 10`.
    - `git diff --stat` for this task lists only the four files.
    - `grep -rn '2026-08-22' .planning/codebase/` prints nothing — plan 06-04's date stamps survived.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'HOLZCLOUD_TEST_REQUIRE_WASM' .planning/codebase/TESTING.md; ls -d tools/*/ | wc -l; grep -c 'tools/wasm' .planning/codebase/STRUCTURE.md</automated>
    <fails_when>the first count is 0, the directory count is not 3, or the third count is 0</fails_when>
    <automated>awk '/^## Beim Weiterarbeiten/{f=1} f' docs/offene-punkte.md | grep -c 'tools/wasm'</automated>
    <fails_when>a count of 0 — the standing instruction about the pin did not land in the standing-instructions section</fails_when>
    <automated>grep -c 'sorted and indented' tools/i18n/main.go; sed -n '/### Phase 6/,/### Phase 7/p' .planning/ROADMAP.md | grep -c 'main\.go:[0-9]'</automated>
    <fails_when>either number is above 0 — the claim survives in the source, or the roadmap still carries a line pointer into a file this phase reshaped</fails_when>
    <automated>git diff --name-only HEAD</automated>
    <fails_when>any path outside `.planning/codebase/TESTING.md`, `.planning/codebase/STRUCTURE.md` and `docs/offene-punkte.md` is listed</fails_when>
  </verify>
  <done>The three documents that describe what this phase built describe it accurately, and the constraint that will bite at the next Go bump is written where the person doing that bump will read it.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Standing gate (QUAL-01, QUAL-02) — the translation count and one pass through the running application</name>
  <read_first>
    - .planning/ROADMAP.md — Phase 6 success criterion 6, verbatim
    - .planning/phases/06-aufr-umen/06-CONTEXT.md — the note that this phase adds no user-visible string, so the translation half is expected to be trivially green and is run anyway
    - plugins/*/plugin.json — which plugin renders on which kind of public page, so the browser pass loads pages that actually execute a rebuilt guest
  </read_first>
  <files>(none — this task records evidence, it changes no file)</files>
  <what-built>The whole of Phase 6 is committed: the catalogue writer goes through `encoding/json` and is locked by a round-trip test, all ten guest build artifacts were rebuilt with one pinned compiler, CI rebuilds and compares them before it runs a test, the five self-skipping tests now fail on a runner, and the seven codebase maps plus the working list read true against the tree. The binary is built with `CGO_ENABLED=0 go build -trimpath ./cmd/holzcloud` and started against a scratch data directory on a free port, and the Playwright MCP browser is pointed at it — all of that is automated before this checkpoint is reached, not asked of the reviewer.</what-built>
  <how-to-verify>
    1. Translation half — the SUMMARY quotes the final line of `go run ./tools/i18n` verbatim. Confirm it reports zero open and zero orphaned. This phase adds no user-visible string, so a green line is expected; it is run because that is what makes the number mean something in Phases 7 through 10.
    2. Browser half — the SUMMARY lists the pages loaded against the locally started binary, with a screenshot each: the admin sign-in page, and one public page for each rebuilt guest that renders visibly (search, contact form, year, order). Confirm each screenshot shows the plugin's own output rather than an error placeholder.
    3. Any guest that could not be exercised because a fresh database has no content for it is listed with that reason. Confirm the list is honest rather than empty by assumption.
    4. `go test ./...` and `go run ./tools/wasm -check` both exit 0 at the moment the gate is recorded.
  </how-to-verify>
  <resume-signal>Reply `approved` if the translation line reports zero open and zero orphaned and every screenshot shows working plugin output. Otherwise describe what the screenshots show — a recompiled guest that renders differently is a real finding and belongs in the phase, not in Phase 7.</resume-signal>
  <action>
Run the phase's closing gate and record the result. It has two halves and both are
run, even though one of them is expected to be uneventful.

**Translation half.** Run `go run ./tools/i18n` and record the final line verbatim.
It must report zero open and zero orphaned. This phase adds no user-visible string,
so a green result here is expected — and running it anyway is exactly what makes the
number mean something when Phases 7 through 10 start adding strings. If it is not
green, that is a finding from an earlier phase surfacing, and it goes to the
orchestrator rather than being fixed silently here.

**Browser half.** The phase touches no admin screen and adds no user-visible string,
so there is no new interface to review. What there *is* — and it is not nothing — is
that four of the six rebuilt guest modules render on public pages through the wazero
host. A compiler change is a behaviour change until somebody looks. So the browser
pass is a smoke pass over the rebuilt guests in the running application, not an
invented UAT script for screens that did not change.

Drive it with the Playwright MCP tools against a locally started binary; do not hand
a manual checklist back. Build the binary, start it against a scratch data directory
on a free port, and load: the admin sign-in page (proving the application starts and
serves at all), and one public page for each plugin that has a visible rendering —
the search plugin, the contact form plugin, the year plugin and the order plugin.
For each, confirm the plugin's own output is on the page rather than an error
placeholder, and capture a screenshot. Where a plugin needs content that does not
exist in a fresh database, say so in the SUMMARY rather than inventing a pass; a
guest that could not be exercised is recorded as not exercised.

Record in the SUMMARY: the exact final line of the translation report; the list of
pages loaded with their outcome; the screenshots; and any guest that could not be
exercised, with the reason.
  </action>
  <verify>
    <human-check>The SUMMARY carries the verbatim final line of `go run ./tools/i18n` showing zero open and zero orphaned, a list of the public pages loaded with the rebuilt guests and their outcomes, screenshots, and an explicit note for any guest that could not be exercised.</human-check>
  </verify>
  <acceptance_criteria>
    - `go run ./tools/i18n` reports zero open and zero orphaned, and the line is quoted verbatim in the SUMMARY.
    - The binary built with `CGO_ENABLED=0 go build -trimpath ./cmd/holzcloud` starts against a scratch data directory and serves the admin sign-in page.
    - At least one public page per visibly-rendering plugin was loaded in a real browser and its plugin output confirmed present, or the reason it could not be exercised is recorded.
    - Screenshots are attached or referenced in the SUMMARY.
    - `go test ./...` exits 0 and `go run ./tools/wasm -check` exits 0 at the moment the gate is recorded.
  </acceptance_criteria>
  <done>The translation number is a measured fact rather than an assumption, and the rebuilt guest modules have been seen working in a browser rather than only in the test suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process environment → test outcome | one variable decides whether a missing artifact is fatal; a strict value comparison would silently keep skipping |
| runner → contributor machine | the same test suite must behave differently in the two places, and only the environment distinguishes them |
| rebuilt guest module → the running application | four guests execute inside the wazero host and render on public pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-21 | Spoofing | a self-skipping test reporting green while checking nothing | high | mitigate | The shared helper fails when `HOLZCLOUD_TEST_REQUIRE_WASM` is non-empty, and the three workflows that run tests set it. Both branches are executed and recorded before the task closes |
| T-06-22 | Spoofing | a strict value comparison silently restoring the skip on a runner | medium | mitigate | The helper compares against emptiness, not against the literal value; acceptance requires `true` to behave like `1` |
| T-06-23 | Elevation of privilege | a recompiled guest behaving differently inside the wazero host on a real page | medium | mitigate | The browser pass loads a public page per visibly-rendering guest; a guest that could not be exercised is recorded as not exercised rather than assumed fine |
| T-06-24 | Tampering | the variable leaking into the image workflow, which runs no tests | low | accept | Deliberate exclusion, asserted by a criterion and stated in the SUMMARY so it is not "fixed" later |
| T-06-25 | Information disclosure | the failure message naming a repository-internal path | low | accept | The message is emitted only by the test binary, never by the application |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed; the new package imports `os` and `testing` only. `06-RESEARCH.md` §"Package Legitimacy Audit" records an empty table |
</threat_model>

<verification>
- `go build ./... && go vet ./... && gofmt -l .` clean, `go test ./...` exits 0.
- With a guest module moved aside: the suite skips with the variable unset and fails with it set, in both cases naming `go run ./tools/wasm`.
- The variable is present at workflow level in the three testing workflows and absent from the image workflow; all four files parse.
- `go run ./tools/wasm -check` exits 0.
- `go run ./tools/i18n` reports zero open and zero orphaned.
- The rebuilt guests were driven through a browser and the outcome recorded.
</verification>

<success_criteria>
- MAINT-04: five tests fail loudly on a runner, stay forgiving locally, share one message naming the build command, and were promoted after the rebuild landed.
- MAINT-05 is complete: the three anchors this phase created and the one it invalidated all describe the finished tree, and the ROADMAP note that pointed at a now-deleted claim is retired rather than re-pointed.
- Success criterion 6 is recorded as evidence, both halves.
</success_criteria>

<artifacts_this_phase_produces>
Created or changed by this plan:

| Kind | Name |
|------|------|
| Go package | `internal/plugin/wasmtest` |
| File | `internal/plugin/wasmtest/wasmtest.go` |
| Exported Go function | the module-reading helper in `internal/plugin/wasmtest`, taking `*testing.T` and a path, returning `[]byte` |
| Go constant | the package-level build instruction naming `go run ./tools/wasm` |
| Env var | `HOLZCLOUD_TEST_REQUIRE_WASM`, workflow-level in `ci.yml`, `security.yml`, `release.yml`; read only by test code, never by `internal/config` |
| Doc line | the `tools/` inventory line in `.planning/codebase/STRUCTURE.md`, now naming three helpers |
| Doc lines | the CI and security workflow descriptions in `.planning/codebase/TESTING.md` |
| Doc bullet | the toolchain-pin standing instruction in `docs/offene-punkte.md` `## Beim Weiterarbeiten` |
| Doc note | the Phase 6 i18n planning note in `.planning/ROADMAP.md`, retired — both sub-clauses stamped closed and the line pointer removed |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-aufr-umen/06-07-SUMMARY.md` when done.
</output>
