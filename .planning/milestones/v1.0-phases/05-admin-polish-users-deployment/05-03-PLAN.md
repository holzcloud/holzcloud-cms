---
phase: 05-admin-polish-users-deployment
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - deploy/holzcloud.service
  - deploy/Caddyfile.example
  - deploy/backup.sh
  - deploy/DEPLOY.md
autonomous: true
requirements:
  - DEP-01
  - DEP-02
  - DEP-03
  - DEP-04

must_haves:
  truths:
    - "systemd unit file has security hardening flags"
    - "Caddy config shows reverse proxy with automatic HTTPS"
    - "Backup script uses sqlite3 .backup command (not file copy)"
    - "DEPLOY.md documents full setup from fresh Pi 5"
  artifacts:
    - path: "deploy/holzcloud.service"
      provides: "systemd unit with security hardening"
      contains: "NoNewPrivileges=true"
    - path: "deploy/Caddyfile.example"
      provides: "Caddy reverse proxy config"
      contains: "reverse_proxy"
    - path: "deploy/backup.sh"
      provides: "Backup script for SQLite + media + templates"
      contains: ".backup"
    - path: "deploy/DEPLOY.md"
      provides: "Complete deployment guide for Pi 5"
      contains: "systemctl"
  key_links: []
---

<objective>
Production deployment configuration: systemd unit, Caddy reverse proxy, backup script, and comprehensive deployment guide.

Purpose: Ship the CMS with everything needed to run it on a Raspberry Pi 5 in production.
Output: deploy/ directory with service file, Caddy config, backup script, and setup guide.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-admin-polish-users-deployment/05-CONTEXT.md
@.planning/phases/05-admin-polish-users-deployment/05-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deployment files — systemd, Caddy, backup, guide</name>
  <read_first>
    - CLAUDE.md
    - .planning/phases/05-admin-polish-users-deployment/05-RESEARCH.md
    - .planning/phases/05-admin-polish-users-deployment/05-CONTEXT.md
  </read_first>
  <files>deploy/holzcloud.service, deploy/Caddyfile.example, deploy/backup.sh, deploy/DEPLOY.md</files>
  <action>
1. **`deploy/holzcloud.service`** (per D-18): systemd unit file exactly as specified in RESEARCH.md. Include all security hardening flags: `NoNewPrivileges=true`, `ProtectSystem=strict`, `ProtectHome=true`, `ReadWritePaths=/opt/holzcloud/data`, `PrivateTmp=true`, `ProtectKernelTunables=true`, `ProtectKernelModules=true`, `ProtectControlGroups=true`, `RestrictSUIDSGID=true`, `RestrictNamespaces=true`, `SystemCallArchitectures=native`. Working directory `/opt/holzcloud`. Restart on failure with 5s delay.

2. **`deploy/Caddyfile.example`** (per D-19): Caddy reverse proxy config with comments explaining multi-domain setup. Show one domain block with `reverse_proxy localhost:8080`. Add commented second domain block. Note that Caddy handles TLS automatically.

3. **`deploy/backup.sh`** (per D-20): Executable bash script. Use `sqlite3 .backup` command (not file copy — per STATE.md known risk about WAL). Back up `data/media/` and `data/templates/` directories. Timestamped backup directory. Include retention note (comment suggesting cron + find to prune old backups).

4. **`deploy/DEPLOY.md`** (per D-21): Comprehensive guide written for someone SSHing into a fresh Pi 5. Sections:
   - Prerequisites (Go not needed on Pi — binary is pre-compiled)
   - Build (on dev machine): `CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build -trimpath -ldflags="-s -w" ./cmd/holzcloud`
   - Transfer binary to Pi via scp
   - Create system user: `sudo useradd -r -s /usr/sbin/nologin holzcloud`
   - Create directory structure: `/opt/holzcloud/` and `/opt/holzcloud/data/`
   - Copy binary and deploy files
   - Install and enable systemd service
   - Install Caddy (apt on Debian/Ubuntu), copy Caddyfile, reload
   - Environment variables (PORT, DATA_DIR, LOG_LEVEL, HOLZCLOUD_SECURE=true)
   - First run: visit https://your-domain/admin to create initial admin
   - Backup setup: cron example running backup.sh daily
   - USB SSD recommendation (per STATE.md known risk about SD card fsync unreliability)
   - Updating: stop service, replace binary, start service
   - Troubleshooting: journalctl, common issues
  </action>
  <verify>
    <automated>cd /Users/holz/Projects/Holzcloud_CMS && test -f deploy/holzcloud.service && test -f deploy/Caddyfile.example && test -f deploy/backup.sh && test -f deploy/DEPLOY.md && grep -q "NoNewPrivileges" deploy/holzcloud.service && grep -q "reverse_proxy" deploy/Caddyfile.example && grep -q ".backup" deploy/backup.sh && grep -q "systemctl" deploy/DEPLOY.md && grep -q "USB SSD\|usb ssd\|SSD" deploy/DEPLOY.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `deploy/holzcloud.service` contains all security hardening flags from D-18
    - `deploy/Caddyfile.example` shows reverse_proxy to localhost:8080 with multi-domain comments
    - `deploy/backup.sh` uses `sqlite3 .backup` (not cp), backs up media and templates dirs
    - `deploy/DEPLOY.md` covers build, transfer, systemd setup, Caddy setup, first run, backup cron, USB SSD recommendation, and updating
  </acceptance_criteria>
  <done>deploy/ directory complete with all four files. Guide covers full lifecycle from build to backup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network -> Caddy -> holzcloud | TLS termination at Caddy, plain HTTP to app |
| filesystem | Binary and data directory permissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Elevation of Privilege | systemd service | mitigate | Security hardening flags: NoNewPrivileges, ProtectSystem=strict, dedicated non-login user, ReadWritePaths restricted to data dir only |
| T-05-09 | Information Disclosure | Backup files | mitigate | Document in DEPLOY.md: backup dir permissions should be 700, owned by root or backup user. Backup contains password hashes. |
| T-05-10 | Tampering | SQLite backup corruption | mitigate | Use sqlite3 .backup command which handles WAL correctly, never raw file copy |
</threat_model>

<verification>
1. All four files exist in deploy/
2. systemd unit has security hardening flags
3. Caddy config has reverse_proxy
4. Backup script uses .backup command
5. DEPLOY.md is comprehensive (build through backup)
</verification>

<success_criteria>
- deploy/ directory contains holzcloud.service, Caddyfile.example, backup.sh, DEPLOY.md
- systemd unit hardened with at least 8 security flags
- Backup uses sqlite3 .backup (WAL-safe)
- Guide written for fresh Pi 5 setup, mentions USB SSD recommendation
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-polish-users-deployment/05-03-SUMMARY.md`
</output>
