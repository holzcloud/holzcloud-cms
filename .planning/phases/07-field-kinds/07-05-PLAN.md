---
phase: 07-field-kinds
plan: 05
type: execute
wave: 5
depends_on: ["07-04"]
files_modified:
  - internal/field/field.go
  - internal/field/render.go
  - internal/field/field_test.go
  - internal/admin/page_fields.go
  - internal/admin/page.go
  - internal/admin/page_fields_kinds_test.go
  - internal/public/pagedata.go
  - internal/public/handler_test.go
  - cmd/holzcloud/templates/admin/field_input.html
autonomous: true
requirements: [FIELD-03]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A Term field offers the tags the website already carries as a chooser rather than a free-text box (FIELD-03)"
    - "A Term field stores the term's slug and prints the term's name, so renaming the term changes what the page shows without touching the page (FIELD-03, D-09, REQUIREMENTS.md:294)"
    - "A Term field is absent from a block kind's field chooser, for the same reason a reference field is: a block freezes to HTML when the page is saved, so a value that must follow a later rename cannot live in one (FIELD-03, D-06)"
    - "A Term field's chooser offers only this website's terms; a slug from another website resolves to nothing (FIELD-03)"
    - "A Term field with no stored slug resolves to a typed nil and is left out of the field list, and a stored slug whose term was deleted resolves the same way — a theme's {{with}} prints nothing rather than a stale name (FIELD-03 empty edge)"
    - "Term resolution compares the stored slug with the term's slug by exact Go string equality, never case-insensitively and with no Unicode normalisation, so a slug differing only in case does not resolve and yields the typed nil (FIELD-03 encoding edge)"
    - "BlockKinds() excludes exactly two kinds beside a group and a heading — the reference and the Term — and its capacity hint tracks that count (D-06)"
  artifacts:
    - path: "internal/field/field.go"
      provides: "KindTerm, its Kinds row, its Check case, the BlockKinds exclusion"
      contains: "KindTerm"
    - path: "internal/field/render.go"
      provides: "field.Term, field.TermLookup, Links.Term, Entry.Term and the Resolve case"
      contains: "TermLookup"
    - path: "internal/admin/page_fields.go"
      provides: "TermChoice, pool.terms, the oneView case and the siteTerms loader"
      contains: "func (h *Handler) siteTerms"
    - path: "internal/public/pagedata.go"
      provides: "the term lookup handed to Resolve for the website being rendered"
      contains: "fieldTerms"
    - path: "cmd/holzcloud/templates/admin/field_input.html"
      provides: "the Term chooser select"
      contains: "schlagwort"
  key_links:
    - from: "internal/public/pagedata.go"
      to: "internal/field/render.go"
      via: "ownFields builds a field.Links carrying Term alongside Image and Page"
      pattern: "Term:\\s*h\\.fieldTerms"
    - from: "internal/admin/page_fields.go"
      to: "internal/term/store.go"
      via: "siteTerms calls the term store's ListAll for this website only"
      pattern: "ListAll"
    - from: "cmd/holzcloud/templates/admin/field_input.html"
      to: "internal/admin/page_fields.go"
      via: "the Term select ranges over FieldView.Terms and compares each Slug with the stored value"
      pattern: "\\.Terms"
---

<objective>
Terms as a field kind. `KindRef` is a complete end-to-end template for it and
D-09 copies it wholesale: a chooser instead of a free-text box, a stable identity
in storage, a display value resolved at render time, and an exclusion from block
kinds because a block cannot keep the promise the kind makes.

One thing differs, and it is the whole point. A reference stores an id; a Term
stores the term's **slug**. Renaming a term already keeps its address, so the
slug is the stable identity — and an id cannot be typed into a spreadsheet cell,
which matters because Phase 9's CSV importer will meet these values. The page
prints the term's **name**, resolved fresh on every render, so renaming the term
changes what every page shows without any page being touched.

This work is independent of everything in plans 07-01 through 07-04 and of the
button row in 07-06. It sits here in the order only because it shares three files
with its neighbours — `internal/field/field.go`, `internal/field/render.go` and
`field_input.html` — and two agents editing one kind list is not parallelism.

Purpose: close FIELD-03.
Output: `KindTerm`, its chooser, its resolution, and its one exclusion.
</objective>

<execution_context>
@/Users/holz/.claude/gsd-core/workflows/execute-plan.md
@/Users/holz/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-field-kinds/07-CONTEXT.md
@.planning/phases/07-field-kinds/07-PATTERNS.md
@.planning/phases/07-field-kinds/07-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KindTerm in internal/field — the kind, the exclusion, and the resolution</name>
  <files>internal/field/field.go, internal/field/render.go, internal/field/field_test.go</files>
  <read_first>
    - internal/field/field.go — the kind const block (24-49) and the `KindRef` comment that is the model, the `Kinds` slice, `BlockKinds` (105-118) with its capacity hint and its long doc comment, `Check`'s `case KindRef` (543-549)
    - internal/field/render.go — `Ref` (30-43), `RefLookup` (45-52), `Links` (56-60), `Resolve`'s `case KindRef` (140-158), `Entry` (169-187) with its `Ref *Ref` field, `List`'s `case *Ref` arm (238-243), `Filled`'s `case *Ref` arm
    - internal/term/store.go — `Term` (24-31), its `URL()` method (34), and `ListAll` (234-256)
    - internal/field/field_test.go — the existing tests, for naming and style
    - .planning/phases/07-field-kinds/07-CONTEXT.md — D-06 and D-09
    - .planning/phases/07-field-kinds/07-PATTERNS.md — the `render.go` section, which spells out how `KindTerm` differs from `KindRef`
  </read_first>
  <behavior>
    - `BlockKinds()` does not contain `schlagwort`; it still contains everything else this phase added, `code` included
    - `SubKinds()` does contain `schlagwort` — a Term field inside a group is fine, because a group is not frozen to HTML
    - `Check` on a Term field accepts a non-empty slug-shaped value and refuses one containing a space or an uppercase letter, with a German reason
    - `Resolve` on a Term field whose slug is found yields a non-nil `*Term` carrying the term's current name, its slug and its public address
    - `Resolve` on a Term field yields a typed nil `*Term` when the value is empty, when the lookup is nil, and when the lookup does not find the slug
    - `Resolve` compares the stored slug with the term's slug by exact string equality: a slug differing only in case does not resolve
    - `List` skips a Term entry whose resolved value is nil, and for a non-nil one sets `Entry.Term` and `Entry.Text` to the term's **name**
    - `Filled` returns true for a non-nil `*Term`
  </behavior>
  <action>
**The kind.** Add `KindTerm = "schlagwort"` to the const block in
`internal/field/field.go`, with a German doc comment in the register of the
`KindRef` comment — say why this and not a text field: a typed label goes stale
the moment somebody renames the term, and a chosen one follows it. Add its row to
the `Kinds` slice with an `i18n.N(...)` label and hint.

**The one exclusion (D-06).** Add `KindTerm` to `BlockKinds()`'s `switch`,
beside `KindGroup`, `KindSection` and `KindRef` — and **only** `KindTerm`. Change
the capacity hint from three exclusions to four; it tracks the count and is
otherwise quietly wrong. Extend the function's doc comment so it names both
excluded kinds and gives the reason once for both: a block is rendered to HTML
when the page is saved and that HTML is what visitors get, so a value that exists
to survive a rename cannot live in one.

Do **not** exclude anything else. Both of this function's exclusions are silent
if missed and both are silent if added by mistake — the filter is subtractive, so
a kind is available everywhere unless it is named here.

**The resolution.** In `internal/field/render.go`:

- Add `type Term struct { Name, Slug, URL string }` beside `Ref`, with a doc
  comment saying `Name` is the term's name *as it is right now*, not as it was
  when somebody chose it — that sentence is the whole feature.
- Add `type TermLookup func(slug string) (Term, bool)` beside `RefLookup`, with a
  comment saying the lookup is where the website rule lives: a term belongs to
  one website and a slug from another must not resolve.
- Add `Term TermLookup` to `Links`, a third member beside `Image` and `Page`. A
  nil one means the kind resolves to nothing, which is what a caller without a
  term store wants — that is already this struct's documented contract.
- Add `case KindTerm` to `Resolve`, following `case KindRef` exactly minus the
  `ParseInt`: the stored value **is** the identity, so the lookup takes the raw
  string. Bail to `(*Term)(nil)` when the value is empty, when `links.Term` is
  nil, and when the lookup misses. Keep the guard-clause shape and write the same
  kind of comment `KindRef` carries about why nil and not a broken value.
- Add `Term *Term` to `Entry`, beside `Ref *Ref`, with a doc comment saying it is
  set for a Term field whose term is still there.
- Add `case *Term` to `List`'s type switch, mirroring the `*Ref` arm: skip when
  nil, otherwise set `e.Term` and set `e.Text` to the term's **name**. Printing
  the name is what FIELD-03 asks for.
- Add `case *Term` to `Filled`.

**`Check`.** Add `case KindTerm`, mirroring the `KindRef` case including its
comment about where the real check lives: that the term exists and belongs to
this website is decided where terms are known — the editor offers only its own
and the resolver checks. Here it is a slug-shaped string or it is somebody typing
into the form by hand. Reuse the project's existing slug rules rather than
inventing a second spelling of what a slug is.

**The tests — write them first and watch them fail.** Extend
`internal/field/field_test.go` with German-named tests covering every case in this
task's `<behavior>` block. The valuable ones are the four nil paths (empty, nil
lookup, missing slug, wrong case) and the assertion that `BlockKinds()` excludes
exactly `schlagwort` and `verweis` and still contains `code`.
  </action>
  <acceptance_criteria>
    - `internal/field/field.go` contains `KindTerm = "schlagwort"` with a German doc comment and a `Kinds` slice row
    - `BlockKinds()`'s `switch` names `KindGroup`, `KindSection`, `KindRef` and `KindTerm` and nothing else, and its capacity hint subtracts four
    - `BlockKinds()` still contains `code`; a test asserts it
    - `internal/field/render.go` contains `type Term struct`, `type TermLookup func`, `Term TermLookup` on `Links`, `Term *Term` on `Entry`, and `case KindTerm` in `Resolve`
    - `List` and `Filled` each contain a `case *Term`
    - `go test ./internal/field/` exits 0 and covers the four nil paths and the case-sensitivity of the slug comparison
    - A resolved Term entry's `Text` is the term's name, not its slug
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./internal/field/ && gofmt -l internal/field/</automated>
    <fails_when>non-zero exit, or gofmt -l prints any file path</fails_when>
    <automated>grep -n 'len(Kinds)-3' internal/field/field.go</automated>
    <fails_when>the command prints a line — `BlockKinds()`'s capacity hint still tracks three exclusions after a fourth was added. No output (exit 1) is the pass.</fails_when>
    <automated>go test ./internal/field/ -v</automated>
    <fails_when>non-zero exit, or "no test files" in the output, or any "--- FAIL" line</fails_when>
  </verify>
  <done>A Term field is a kind, is refused inside a block kind, and resolves a stored slug to the term's current name or to nothing at all.</done>
</task>

<task type="auto">
  <name>Task 2: The chooser in the page editor</name>
  <files>internal/admin/page_fields.go, internal/admin/page.go, internal/admin/page_fields_kinds_test.go, cmd/holzcloud/templates/admin/field_input.html</files>
  <read_first>
    - internal/admin/page_fields.go — `PageChoice` (97-105), `pool` (107-118) and its doc comment about a third kind of chooser, `oneView` (211-229), `refPages` (236-260)
    - internal/admin/page.go — `PageFormData` around lines 73-90 where `RefPages` is declared, and line 339 where it is filled
    - internal/term/store.go — `ListAll` (234-256), which already returns exactly `{ID, Slug, Name}` for one website
    - internal/admin/handler.go — line 50, where `terms *term.Store` already hangs on the handler
    - cmd/holzcloud/templates/admin/field_input.html — the `verweis` branch, which is the shape to copy, including its `{{$id := .PageID}}` capture
    - internal/admin/page_fields_kinds_test.go — the render test added in plan 07-03, which this task extends
    - .planning/phases/07-field-kinds/07-PATTERNS.md — the `page_fields.go` section, which lists the four places the reference chooser touches
  </read_first>
  <action>
Copy the reference chooser in the four places `07-PATTERNS.md` names, and nowhere
else.

**`internal/admin/page_fields.go`.**

- Add `type TermChoice struct { Slug, Name string }` beside `PageChoice`. No
  draft equivalent exists for a term, so it carries nothing more.
- Add `terms []TermChoice` to `pool` and to what `PageFormData.pool()` builds.
  The `pool` doc comment already anticipates this: it exists so a third kind of
  chooser does not mean touching every call site. Honour that — no call site of
  `pool()` changes.
- Add `Terms []TermChoice` to `FieldView`, with a doc comment in the register of
  the `Pages` one: it is the choice a Term field offers and is empty for every
  other kind.
- Add `case field.KindTerm` to `oneView`, setting `v.Terms = p.terms`. The stored
  slug is already in `v.Value`, so the template compares against that directly
  and no second field is needed — that is the one way this differs from the
  reference case, which has to parse an id.
- Add `func (h *Handler) siteTerms(ctx context.Context, websiteID int64) []TermChoice`
  beside `refPages`, following it exactly: nil store yields a nil slice, an error
  yields a nil slice and never a failed request, and the result is mapped into
  the view type. It is shorter than `refPages` because `ListAll` already returns
  this website's terms in name order. Do not add a bound: `ListAll` is already
  scoped to one website and a website's label list is not a page list.

**`internal/admin/page.go`.** Add `RefTerms []TermChoice` to `PageFormData`
beside `RefPages`, with a doc comment saying it is the choice a Term field
offers, and fill it beside the `RefPages` line with a call to `siteTerms`.

**`cmd/holzcloud/templates/admin/field_input.html`.** Add an
`{{else if .Is "schlagwort"}}` branch beside the `verweis` branch. A `<select>`
carrying `id="{{.Name}}"`, `name="{{.Name}}"` and `class="form-input form-select"`,
with a first `<option value="">` labelled through `{{t "…"}}` in the register of
the reference branch's "– keine Seite –", then a `{{$slug := .Value}}` capture
and a `{{range .Terms}}` emitting one `<option value="{{.Slug}}">` per term,
`selected` when its slug equals the captured value. The option text is the term's
**name**. Every user-visible string goes through `{{t "…"}}`.

**The test.** Extend `internal/admin/page_fields_kinds_test.go`: define a Term
field on a website that carries two terms, render the page form, and assert the
select contains both term names as option text and both slugs as option values,
that the stored slug's option carries `selected`, and that a term belonging to a
different website appears nowhere in the output. That last assertion is the one
worth having — every resource in this program is scoped to exactly one website.
  </action>
  <acceptance_criteria>
    - `internal/admin/page_fields.go` contains `type TermChoice struct`, `terms []TermChoice` on `pool`, `Terms []TermChoice` on `FieldView`, `case field.KindTerm` in `oneView`, and `func (h *Handler) siteTerms`
    - `grep -c 'func (d PageFormData) pool()' internal/admin/page_fields.go` returns 1 and no other call site of `pool()` changed
    - `internal/admin/page.go` contains `RefTerms` on `PageFormData` and a `siteTerms` call beside the `refPages` call
    - `cmd/holzcloud/templates/admin/field_input.html` contains `{{else if .Is "schlagwort"}}` and ranges over `.Terms`
    - `siteTerms` returns a nil slice when the term store is nil and when `ListAll` returns an error, and never propagates the error
    - `go test ./internal/admin/ -run TestSchlagwortfeld` exits 0 and asserts another website's term appears nowhere in the rendered form
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./internal/admin/ && gofmt -l internal/admin/</automated>
    <fails_when>non-zero exit, or gofmt -l prints any file path</fails_when>
    <automated>go test ./internal/admin/ -run TestSchlagwortfeld -v</automated>
    <fails_when>non-zero exit, or "testing: warning: no tests to run" in the output, or no "--- PASS" line</fails_when>
    <automated>go test ./internal/admin/ -v</automated>
    <fails_when>non-zero exit, or any "--- FAIL" line — a change to `pool` or `FieldView` broke a neighbouring chooser</fails_when>
  </verify>
  <done>A Term field offers this website's tags as a chooser, shows the stored one as selected, and never shows another website's.</done>
</task>

<task type="auto">
  <name>Task 3: The public page prints the term's current name</name>
  <files>internal/public/pagedata.go, internal/public/handler_test.go</files>
  <read_first>
    - internal/public/pagedata.go — `ownFields` (58-74) where the `field.Links` is built, `fieldRefs` (77-115) as the shape to copy including its caching and its website check, `fieldImages` just below it, and `labelsForPage` (138)
    - internal/public/handler.go — line 38, where `termStore *term.Store` already hangs on the handler, and `SetTermStore` at 104
    - internal/public/tag.go — `termLinksAt` (123) and `localeTermLinks` (135), which show how a term's public address gets its language prefix
    - internal/term/store.go — `ListAll` (234) and the `URL()` method (34)
    - internal/public/handler_test.go — the existing public render tests, for the harness and naming
    - internal/field/render.go — `TermLookup` and `case KindTerm` as added in Task 1
  </read_first>
  <action>
**The lookup.** Add `func (h *Handler) fieldTerms(r *http.Request, websiteID int64) field.TermLookup`
to `internal/public/pagedata.go`, beside `fieldRefs` and `fieldImages` and in
their exact shape:

- return a closure over a small cache, so a page carrying ten Term fields does
  not make ten queries;
- fill the cache once from the term store's `ListAll` for **this** website — that
  single call is the website rule, and it is why a slug belonging to another site
  cannot resolve;
- return `false` when the term store is nil and when the slug is not in the map,
  so `Resolve` yields its typed nil and a theme's `{{with}}` leaves the block out;
- build the `URL` with the same language prefix the term links on the rest of the
  page already get — follow `termLinksAt` and `localeTermLinks` rather than
  writing `/tag/` out again here.

The comparison is exact string equality on the slug. Do not fold case and do not
normalise: a slug is already the normalised spelling of a name, and a second
normalisation here would make two different terms collide.

**The wiring.** Add `Term: h.fieldTerms(r, websiteID)` to the `field.Links` built
in `ownFields`, beside `Image` and `Page`. That one line is what makes the whole
kind work on the public side; without it every Term field resolves to nothing and
nothing anywhere reports why.

**The test.** Add a test to `internal/public/handler_test.go` in the file's own
style: publish a page carrying a Term field whose value is a term's slug, render
it, assert the term's **name** appears in the output; then rename the term in the
database **without touching the page**, render again, and assert the new name
appears and the old one does not. That before-and-after pair is FIELD-03's actual
promise and the only assertion that proves it. Add a second case: delete the
term and assert the page still renders and shows nothing for that field.
  </action>
  <acceptance_criteria>
    - `internal/public/pagedata.go` contains `func (h *Handler) fieldTerms` and the `field.Links` built in `ownFields` carries `Term:`
    - `grep -c '"/tag/"' internal/public/pagedata.go` returns 0 — the address is built through the existing helper, not spelled out again
    - `fieldTerms` returns a lookup reporting false when `h.termStore` is nil
    - `go test ./internal/public/ -run TestSchlagwortfeld` exits 0
    - Renaming a term changes what an already-saved page prints, with no write to the page
    - Deleting a term leaves the page rendering, with nothing printed for that field
  </acceptance_criteria>
  <verify>
    <automated>go build ./... && go vet ./internal/public/ && gofmt -l internal/public/</automated>
    <fails_when>non-zero exit, or gofmt -l prints any file path</fails_when>
    <automated>go test ./internal/public/ -run TestSchlagwortfeld -v</automated>
    <fails_when>non-zero exit, or "testing: warning: no tests to run" in the output, or fewer than two "--- PASS" lines</fails_when>
    <automated>go test ./internal/public/</automated>
    <fails_when>non-zero exit — the widened `field.Links` broke an existing public render path</fails_when>
  </verify>
  <done>A page prints the term's name, renaming the term changes what the page shows without the page being touched, and deleting it leaves the page intact.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
The complete ledger of symbols Phase 7 creates. Anything named here is new in
this phase and is not drift against the existing tree.

**`internal/field` — new identifiers**
- Kind constants: `KindTime = "zeit"`, `KindRange = "bereich"`, `KindCode = "code"`, `KindMulti = "mehrfachauswahl"`, `KindTerm = "schlagwort"`
- Display constant: `DisplayButtons = "knopfreihe"` (an empty `Display` is the dropdown that exists today)
- `func SplitValues(raw string) []string`, `func JoinValues(values []string) string`
- `func (d Def) IsMultiValued() bool`, `func (d Def) NameSuffix() string`, `func (d Def) IsButtonRow() bool`
- `Def` fields: `Display string`, `MaxValues int`, `RangeMin string`, `RangeMax string`
- `Entry` fields: `Values []string`, `Term *Term`
- `type Term struct { Name, Slug, URL string }`, `type TermLookup func(slug string) (Term, bool)`, `Links.Term TermLookup`
- `MayControl()` gains one exclusion, `KindTime`; `BlockKinds()` gains one, `KindTerm`
- `Check` gains cases for `KindTime`, `KindRange`, `KindMulti` and `KindTerm`, plus a shared byte-budget guard

**`internal/admin` — new identifiers**
- `type TermChoice struct { Slug, Name string }`
- `func (h *Handler) siteTerms(ctx context.Context, websiteID int64) []TermChoice`
- `pool.terms []TermChoice`
- `PageFormData.RefTerms []TermChoice`
- `FieldView` fields: `Selected []string`, `Terms []TermChoice`
- `func (v FieldView) Buttons() bool`
- `parseRowName` — widened to report whether a row's sub-field carried the multi-value marker
- `switchOf` — widened from `switchOf(kind string)` to `switchOf(d field.Def)`, new return value `"knopfreihe"`

**`internal/public` — new identifiers**
- `func (h *Handler) fieldTerms(r *http.Request, websiteID int64) field.TermLookup`

**`internal/block` — changed behaviour**
- `renderOwn` gains `case field.KindCode`; `PlainText`'s kind whitelist gains `field.KindCode` and `field.KindMulti`

**Database — migration `internal/db/migrations/00046_field_kinds.sql`**
- `page_field_defs.darstellung TEXT NOT NULL DEFAULT ''`
- `page_field_defs.max_werte INTEGER NOT NULL DEFAULT 0`
- `page_field_defs.min_wert TEXT NOT NULL DEFAULT ''`
- `page_field_defs.max_wert TEXT NOT NULL DEFAULT ''`

**Templates and stylesheet**
- `cmd/holzcloud/templates/admin/field_input.html` — new branches for `zeit`, `bereich`, `code`, `mehrfachauswahl`, `schlagwort`, plus the button-row branch inside the existing `auswahl` branch and its hidden sentinel
- `cmd/holzcloud/templates/admin/field_list.html` — new definition-form controls named `darstellung`, `max_werte`, `min_wert`, `max_wert`
- `cmd/holzcloud/assets/admin.css` — new classes `.form-code`, the button-row container class, and `.feld-schalter--knopfreihe`

**Bundle format** — new JSON keys on `internal/bundle/format.go`'s `Field`: `display`, `max_values`, `min`, `max`

**Contract documents** — new per-kind sections and the `.Values` / `.Term` rows in `internal/tmplspec/TEMPLATE-SPEC.md`, and new `Entry` rows in `SampleData` and `MinimalData` in `internal/template/sample.go`

**New test files** — `internal/field/values_test.go`, `internal/field/store_test.go`, `internal/admin/page_fields_multi_test.go`, `internal/admin/page_fields_kinds_test.go`, `internal/admin/page_fields_switch_test.go`

**i18n** — new German source strings reachable through `{{t "…"}}` and `i18n.N(...)`, written into the catalogues by `go run ./tools/i18n -write` followed by `go run ./tools/i18n -schweiz`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → page form (Term field) | The submitted value is a slug string, not an id from a list the server minted. |
| stored slug → term lookup | The lookup decides which website's terms a page may reach. |
| term name → public page | The name is operator content rendered into a public page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-19 | Elevation of Privilege | `fieldTerms` in `internal/public/pagedata.go` | high | mitigate | The lookup is filled from a single `ListAll` scoped to the website being rendered, so a slug belonging to another website is simply not in the map and resolves to the typed nil. This is the same shape `fieldRefs` already uses for pages, and it is the reason the check lives in the lookup rather than in `Check`. A test asserts a foreign website's term resolves to nothing. |
| T-07-20 | Elevation of Privilege | `siteTerms` in `internal/admin/page_fields.go` | high | mitigate | The chooser is filled from `ListAll` for the website the handler already resolved through `lookupWebsite`. A test asserts another website's term appears nowhere in the rendered form. |
| T-07-21 | Elevation of Privilege | a term's name rendered on a public page | medium | mitigate | The name reaches the page as `Entry.Text`, a plain `string`, and is escaped contextually by `html/template`. No `template.HTML` cast exists on this path, and the term name is never routed through the Markdown renderer. |
| T-07-22 | Tampering | a Term field inside a block kind | high | mitigate | `BlockKinds()` excludes `KindTerm`, and the store already refuses a field whose kind is not in that list. Without the exclusion a block would freeze the term's name into HTML at save time and a later rename would leave a stale label with nothing reporting it. A test asserts the exclusion. |
| T-07-23 | Denial of Service | `fieldTerms` caching | low | mitigate | The lookup fills its map once per rendered page from one query, so a page carrying many Term fields costs one round trip, matching what `fieldRefs` and `fieldImages` already do. |
| T-07-SC | Tampering | package-manager installs | low | accept | No `go get`, `npm`, `pip` or `cargo` install task exists in this phase — every symbol is Go standard library or already in `go.mod`. The package-legitimacy gate does not fire, and no legitimacy checkpoint is needed. |
</threat_model>

<verification>
- `go build ./...` succeeds
- `go test ./internal/field/ ./internal/admin/ ./internal/public/` passes
- `BlockKinds()` excludes exactly the reference and the Term, and still contains `code`
- Renaming a term changes what an already-saved page prints
- No other website's term is reachable from either the chooser or the resolver
</verification>

<success_criteria>
- A Term field offers this website's tags as a chooser, stores the slug and prints the name
- Renaming a term changes what the page shows without touching the page
- Deleting a term leaves the page rendering with nothing printed for that field
- A Term field is absent from a block kind's field chooser
</success_criteria>

<output>
Create `.planning/phases/07-field-kinds/07-05-SUMMARY.md` when done
</output>
</content>
