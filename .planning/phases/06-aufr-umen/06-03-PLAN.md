---
phase: 06-aufr-umen
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STACK.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/TESTING.md
  - .planning/codebase/CONVENTIONS.md
  - docs/offene-punkte.md
  - .planning/quick/260902-cml-i18n-kataloge-sauber-0-offen-0-verwaist/deferred-items.md
  - .planning/quick/260903-ceo-beispiel-bundle-statt-kundendaten/deferred-items.md
  - .planning/quick/260903-da5-kleinigkeiten-aus-der-freigabepruefung/deferred-items.md
autonomous: true
requirements: [MAINT-05]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every countable fact in the seven `.planning/codebase/` maps — migration count and last migration name, Go version, package and theme inventories, file counts, dependency versions, the deployment target — matches what a command run against the working tree reports."
    - "Every file-and-line reference in `ARCHITECTURE.md` and `CONVENTIONS.md` points at the symbol it names."
    - "`INTEGRATIONS.md` describes only infrastructure that exists: no Kubernetes directory, no deployment workflow that was deleted, no arm64 cross-compile job."
    - "`docs/offene-punkte.md` names migration `00045`, no longer lists the finished Dependabot item among the numbered work items, and keeps its procedure as a standing instruction."
    - "All three `deferred-items.md` read as closed at a glance, with their finding text intact."
  artifacts:
    - .planning/codebase/ARCHITECTURE.md
    - .planning/codebase/STACK.md
    - .planning/codebase/STRUCTURE.md
    - .planning/codebase/INTEGRATIONS.md
    - .planning/codebase/CONCERNS.md
    - .planning/codebase/TESTING.md
    - .planning/codebase/CONVENTIONS.md
    - docs/offene-punkte.md
  key_links:
    - "each corrected number → the command that proves it (`ls … | wc -l`, `grep -n 'func …'`, `grep '^go ' go.mod`, `go list -m`), recorded beside the correction in the SUMMARY."
    - "`06-RESEARCH.md` §\"MAINT-05 Correction Inventory\" → the checklist this plan executes, re-verified against HEAD before it is trusted."
---

<objective>
Make every planning note Phases 7–10 are planned against read true against the
working tree.

Purpose: MAINT-05, widened by D-18 from one map to all seven. The maps are read by
planners, not by code, so nothing fails when they drift — which is why they drifted
for six weeks. Corrections are surgical (D-19): a countable fact is replaced by the
number a command reports, a moved line reference by the line the symbol is on now,
and nothing else is touched. The maps are never regenerated; re-running the mapper
would produce an unreviewable diff and would silently rewrite the hand-written
judgements in `CONCERNS.md`, the one file whose value is assessment rather than
counting.

Output: seven corrected maps, a `docs/offene-punkte.md` whose numbered list contains
only open work, and three closed findings stamped as closed with their text intact.

**Deliberately out of scope, and left to plan 06-06:** the three anchors this phase
itself changes — the `tools/` inventory line in `STRUCTURE.md`, the CI description
in `TESTING.md`, and a new standing instruction about the toolchain pin in
`docs/offene-punkte.md`. Correcting them here would make them false again the
moment the later plans land.
</objective>

<execution_context>
@/Users/holz/.claude/gsd-core/workflows/execute-plan.md
@/Users/holz/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-aufr-umen/06-CONTEXT.md
@.planning/phases/06-aufr-umen/06-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the seven codebase maps surgically against a re-verified inventory (D-18, D-19)</name>
  <precondition>`06-RESEARCH.md`'s correction inventory was verified against commit `8edcd9cb0ac8350c7916f43c50882b3d10af1e1b`. `HEAD` has moved since. Every line number in the inventory must be re-derived against the current `HEAD` before it is written into a map; a line reference copied from the research and not re-checked is the exact defect this task exists to remove.</precondition>
  <read_first>
    - .planning/phases/06-aufr-umen/06-RESEARCH.md — §"MAINT-05 Correction Inventory", sections A through G. This is the specification for this task.
    - .planning/codebase/ARCHITECTURE.md — the whole file
    - .planning/codebase/STACK.md — the whole file
    - .planning/codebase/STRUCTURE.md — the whole file
    - .planning/codebase/INTEGRATIONS.md — the whole file
    - .planning/codebase/CONCERNS.md — lines 1–10 and 180–250 (the two numbers to correct; the judgements stay)
    - .planning/codebase/TESTING.md — lines 1–10 only
    - .planning/codebase/CONVENTIONS.md — lines 1–10, 20–25, 85–90, 105–115
    - .planning/phases/06-aufr-umen/06-CONTEXT.md — D-18 and D-19
  </read_first>
  <files>.planning/codebase/ARCHITECTURE.md, .planning/codebase/STACK.md, .planning/codebase/STRUCTURE.md, .planning/codebase/INTEGRATIONS.md, .planning/codebase/CONCERNS.md, .planning/codebase/TESTING.md, .planning/codebase/CONVENTIONS.md</files>
  <action>
Work through `06-RESEARCH.md` §"MAINT-05 Correction Inventory" section by section.
For every row, run the command in its evidence column against the current `HEAD`
first, then write what that command reported. Do not copy a number or a line
reference out of the research document without re-running its proof — the research
was measured at `8edcd9cb` and the tree has moved. Where a re-run disagrees with
the research, the re-run wins and the disagreement goes in the SUMMARY.

Section A, `ARCHITECTURE.md`. Correct the countable facts: the goose migration
count and the name of the last migration; the non-test Go file count in
`internal/admin/`; the number of `jobs.Job{` literals registered in
`cmd/holzcloud/main.go`; the deployment-target phrase that still names a Raspberry
Pi, which became linux/amd64 on 2026-09-03 by quick task `x86-statt-arm`; and the
domain-package list, which omits eleven packages that exist under `internal/`.
Then correct the sixteen drifted file-and-line references — `newRouter`, `main`,
`runCLI`, the outer middleware chain, `HandlePage`, `RenderPage`, `serveCached`,
the admin chain, both `ErrHandler` declarations, `cacheKey` and
`InvalidateTemplateCache`, the sharelink signer wiring, the markdown sanitizer, the
public handler reference that now lands inside an unrelated comment, the
`usersExist` declaration, and the `WithSettings`/`WithPages`/`WithRender`/`WithNotify`
call sites — each re-derived with the `grep -n` the inventory names. Leave the
six rows the inventory marks as still correct exactly as they are.

Section B, `STACK.md`. The Go version, the migration count and last migration name,
the theme list (a template shipped on 2026-09-03 and is missing from it), the
primary build target, and six dependency versions. Derive the dependency versions
from `go.mod` or `go list -m` rather than from the research table. Leave the five
rows marked correct alone.

Section C, `STRUCTURE.md`. The count of directories under `internal/`, the goose
file count, the top-level tree entry naming a Kubernetes directory that no longer
exists, the package inventory that omits seven packages, the `internal/admin/`
file count, the claim that every plugin ships a distributable archive (one of the
five does not; it carries a `migrations/` directory instead), and the theme list.
**Do not touch the `tools/` inventory line** — plan 06-06 updates it once
`tools/wasm` exists.

Section D, `INTEGRATIONS.md`, the most drifted of the seven. Remove or rewrite
every claim about infrastructure that does not exist: the Kubernetes manifests, the
Kubernetes secret and its README, the deployment workflow driven by `workflow_run`
and `kubectl rollout status`, and the arm64 cross-compile job. Name what actually
publishes today — the container image workflow to GHCR and the release workflow
triggered on a `v*` tag. Correct the two health-endpoint line references and the
two remaining Raspberry Pi phrases. Leave the security-workflow description, the
backup story, the environment-variable list and the MCP endpoint alone.

Section E, `CONCERNS.md`. Correct exactly three things: the two date stamps, the
migration count with its last migration name, and the Go version inside the
concern about the version mismatch. **Keep that concern open** — the mismatch it
describes is still real, because `CLAUDE.md` still states a much older minimum. It
is the number that is wrong, not the finding. Touch nothing else in this file.

Section F, `TESTING.md`. Correct the date stamp and the Go version on line 8.
**Do not touch the two lines describing `ci.yml` and `security.yml`** — this phase
changes what they describe, and plan 06-06 rewrites them after those changes land.

Section G, `CONVENTIONS.md`. Correct the date stamp and the `ErrHandler` line
reference, which is the same declaration already being corrected in
`ARCHITECTURE.md`. Re-derive the two admin-handler line ranges rather than applying
a fixed offset. Nothing else in this file is wrong.

Across all seven, replace the stale `Analysis Date` stamp with today's date and,
where a file also carries a `refreshed:` key, the same date. Add one short line
under each stamp saying the file was corrected surgically against the working tree
in Phase 6 and not regenerated, so the next reader knows the difference.

Record in the SUMMARY, per file, a table of every changed value with the command
that proved it. That table is what makes D-19's "provable line by line" checkable.
  </action>
  <acceptance_criteria>
    <!-- planner-discipline-allow: 2026-08-22 -->
    <!-- planner-discipline-allow: k8s/ -->
    <!-- planner-discipline-allow: deploy.yml -->
    <!-- planner-discipline-allow: Raspberry Pi -->
    - `grep -rc '2026-08-22' .planning/codebase/` reports 0 for all seven files.
    - `grep -rl 'k8s/' .planning/codebase/` prints nothing.
    - `grep -rl 'deploy.yml' .planning/codebase/` prints nothing.
    - `grep -rl 'Raspberry Pi' .planning/codebase/` prints nothing.
    - The migration count and last migration name in `ARCHITECTURE.md`, `CONCERNS.md`, `STACK.md` and `STRUCTURE.md` all equal what `ls internal/db/migrations/*.sql | wc -l` and `ls internal/db/migrations/*.sql | tail -1` report.
    - The Go version in `STACK.md`, `CONCERNS.md` and `TESTING.md` equals the value in `grep '^go ' go.mod`.
    - Every `cmd/holzcloud/main.go:<n>`, `internal/**:<n>` reference changed in `ARCHITECTURE.md` and `CONVENTIONS.md` points at the named symbol — spot-check each with `sed -n '<n>p' <file>`.
    - `.planning/codebase/CONCERNS.md` still contains the concern about the Go version mismatch with `CLAUDE.md`; only its number changed. `git diff --stat -- .planning/codebase/CONCERNS.md` shows no more than 6 changed lines.
    - `.planning/codebase/TESTING.md` lines describing `ci.yml` and `security.yml` are byte-unchanged: `git diff -- .planning/codebase/TESTING.md` shows no change to any line containing `workflows/`.
    - `.planning/codebase/STRUCTURE.md`'s `tools/` inventory line is byte-unchanged.
    - The SUMMARY carries a per-file table of every changed value with the command that proved it.
  </acceptance_criteria>
  <verify>
    <automated>grep -rn '2026-08-22' .planning/codebase/ ; echo "exit=$?"</automated>
    <fails_when>any file and line is printed, i.e. `exit=0` — a stale analysis stamp survives</fails_when>
    <automated>grep -rn 'k8s/\|deploy\.yml\|Raspberry Pi\|linux/arm64' .planning/codebase/ ; echo "exit=$?"</automated>
    <fails_when>any match is printed — a map still describes infrastructure that was deleted or a target that was retargeted</fails_when>
    <automated>echo "tree: $(ls internal/db/migrations/*.sql | wc -l | tr -d ' ') $(ls internal/db/migrations/*.sql | tail -1 | xargs basename)"; grep -rn '00045_pages_locale_unique' .planning/codebase/ | wc -l</automated>
    <fails_when>the second number is less than 4 — fewer than the four maps that name the last migration were corrected</fails_when>
    <automated>git diff --name-only -- .planning/codebase/</automated>
    <fails_when>fewer than 7 files are listed, or any path outside `.planning/codebase/` appears</fails_when>
  </verify>
  <done>All seven maps report numbers a command reproduces and line references that land on the symbol they name; the corrections are surgical, the assessment file keeps its judgements, and every changed value has a proof recorded beside it.</done>
</task>

<task type="auto">
  <name>Task 2: `docs/offene-punkte.md` — retire the finished Dependabot item and keep its procedure (D-21, D-22)</name>
  <read_first>
    - docs/offene-punkte.md — lines 79–112 (`## 6.` and the whole `## 7.` Dependabot section, including the fenced database-check block and the expected-pragma paragraph), 114 (`## Was bewusst nicht gebaut wird`), 138–152 (`## Beim Weiterarbeiten` and its three existing standing instructions)
    - .planning/phases/06-aufr-umen/06-CONTEXT.md — D-21 and D-22
    - .planning/phases/06-aufr-umen/06-RESEARCH.md — §"H. `docs/offene-punkte.md` (D-21, D-22)", including the finding that the procedure runs further than D-21's estimate and that the measurement inside it is historical and stays as written
  </read_first>
  <files>docs/offene-punkte.md</files>
  <action>
The numbered list in this file is what is missing. A finished item sitting in it
makes the whole list untrustworthy, and its procedure is the part worth keeping.

Remove the Dependabot heading and its status paragraph from the numbered list.
It is the last numbered heading in the file — the list runs from `## 1.` to
`## 7.` — so nothing renumbers, which is why this is a clean removal rather than a
renumbering exercise. Verify that before cutting: run
`grep -n '^## [0-9]' docs/offene-punkte.md` and confirm the Dependabot heading is
the highest-numbered one.

Move the procedure into `## Beim Weiterarbeiten`, the section this document already
keeps for standing instructions and where the translation rule and the browser rule
already live. Move the whole procedure, not D-21's shorter estimate of it: the
paragraph about taking one bump at a time with a build, a vet and a test run
between them and about the go.mod conflict being the normal case; the paragraph
about running the pure-Go SQLite driver against a real database file; the fenced
command block that starts the binary against an empty data directory; and the
paragraph listing the expected pragma values and integrity checks. Keep the
historical measurement line as written — it is a correctly dated observation of a
specific driver version, not a claim about today, and "correcting" it would destroy
the only record of when the check was last run. Reshape it into a bullet matching
the three that are already in that section: a bolded lead-in, then the instruction.

Correct the migration claim in the `## Beim Weiterarbeiten` migration bullet: it
names the migration the tree runs to, and that number is one behind. Derive the
right one from `ls internal/db/migrations/*.sql | tail -1` rather than typing it.

Do not add the toolchain-pin instruction here — plan 06-06 adds it, once
`tools/wasm` exists and the instruction is true.
  </action>
  <acceptance_criteria>
    <!-- planner-discipline-allow: 00044 -->
    <!-- planner-discipline-allow: Dependabot -->
    - `grep -n '^## [0-9]' docs/offene-punkte.md` lists headings 1 through 6 and no seventh.
    - `grep -c '00044' docs/offene-punkte.md` reports 0.
    - The `## Beim Weiterarbeiten` migration bullet names the basename reported by `ls internal/db/migrations/*.sql | tail -1`.
    - The text between `## Beim Weiterarbeiten` and the end of the file contains the substrings `go vet`, `go test`, `HOLZCLOUD_DATA_DIR`, `journal_mode=wal` and `integrity_check`.
    - `grep -c 'Dependabot' docs/offene-punkte.md` reports at least 1, and every occurrence is below the `## Beim Weiterarbeiten` heading — the procedure moved, it was not deleted.
    - The historical measurement sentence naming the driver version and the counts it observed is present and byte-unchanged.
    - `git diff --stat -- docs/offene-punkte.md` shows changes only in this file.
  </acceptance_criteria>
  <verify>
    <automated>grep -n '^## ' docs/offene-punkte.md</automated>
    <fails_when>a numbered heading above 6 is listed, or `## Beim Weiterarbeiten` is absent</fails_when>
    <automated>awk '/^## Beim Weiterarbeiten/{f=1} f' docs/offene-punkte.md | grep -c 'HOLZCLOUD_DATA_DIR'</automated>
    <fails_when>a count of 0 — the database-check procedure did not land in the standing-instructions section</fails_when>
    <automated>echo "tree: $(ls internal/db/migrations/*.sql | tail -1 | xargs basename)"; grep -n 'Migrationen' docs/offene-punkte.md</automated>
    <fails_when>the migration bullet names a migration other than the basename printed on the first line</fails_when>
  </verify>
  <done>The numbered list holds only open work, the Dependabot procedure survives as a standing instruction with its dated measurement intact, and the migration number matches the tree.</done>
</task>

<task type="auto">
  <name>Task 3: Stamp the three `deferred-items.md` as closed, text unchanged (D-20)</name>
  <read_first>
    - .planning/quick/260902-cml-i18n-kataloge-sauber-0-offen-0-verwaist/deferred-items.md — the whole file; the finding is the catalogue-format one, closed by quick task `260903-bsk`
    - .planning/quick/260903-ceo-beispiel-bundle-statt-kundendaten/deferred-items.md — the whole file; the `page.terms` slug-versus-name finding
    - .planning/quick/260903-da5-kleinigkeiten-aus-der-freigabepruefung/deferred-items.md — the whole file; the flaky form test
    - .planning/phases/06-aufr-umen/06-CONTEXT.md — D-20
    - .planning/phases/06-aufr-umen/06-RESEARCH.md — §"I. The three `deferred-items.md` (D-20)", the table naming what closed each one
    - .planning/STATE.md — the Quick Tasks Completed table, rows `260903-zwei-abgelegte-fehler` and the i18n rows, which name the commits and tasks the stamps cite
  </read_first>
  <files>.planning/quick/260902-cml-i18n-kataloge-sauber-0-offen-0-verwaist/deferred-items.md, .planning/quick/260903-ceo-beispiel-bundle-statt-kundendaten/deferred-items.md, .planning/quick/260903-da5-kleinigkeiten-aus-der-freigabepruefung/deferred-items.md</files>
  <action>
MAINT-05 asks that these read as closed. A deleted file reads as nothing, and each
of these carries reasoning a later phase needs — the slug-versus-name finding bears
directly on future bundle work. So the text stays and a stamp goes on top (D-20).

In each of the three files, insert one blockquote line immediately after the `#`
heading on the first line, before the first `##` finding heading. Form:
a bold `Erledigt am <date> — <what closed it>` inside a `> ` blockquote, in German,
matching the register of the file it sits in. One line, not a paragraph.

What each stamp names:

- The i18n one: the catalogue format was normalised by quick task `260903-bsk` on
  2026-09-03; the tool's flush-left format is canonical and the deviation was
  recorded as waived entry 1 in `.planning/WINDOWS.md`. Of the two routes the note
  itself offers, the first was taken.
- The bundle one: `tools/mkbundle` now checks tags against their names rather than
  their identifiers, closed by quick task `260903-zwei-abgelegte-fehler`.
- The flaky-test one: the assertion in `internal/public/formular_e2e_test.go` was
  rewritten to check the form's shape rather than two characters in the rendered
  noise, closed by the same quick task, four hundred runs green.

Confirm each closure against the tree before writing the stamp — the mkbundle tag
check and the rewritten assertion are both greppable — and record the confirming
command in the SUMMARY. Change nothing below the stamp: no heading, no body
sentence, no list item.
  </action>
  <acceptance_criteria>
    - Each of the three files has a line beginning `> **Erledigt am ` positioned after line 1 and before the first `## ` heading.
    - Each stamp names either a quick-task identifier or a commit.
    - `git diff -U0 -- '.planning/quick/*/deferred-items.md'` shows only added lines and no removed lines across all three files.
    - `git diff --numstat -- '.planning/quick/*/deferred-items.md'` reports 0 deletions for each of the three files.
    - The confirming commands are recorded in the SUMMARY: a grep showing `tools/mkbundle` comparing against the tag name, and a grep showing the rewritten assertion in `internal/public/formular_e2e_test.go`.
  </acceptance_criteria>
  <verify>
    <automated>grep -c '^> \*\*Erledigt am ' .planning/quick/260902-cml-i18n-kataloge-sauber-0-offen-0-verwaist/deferred-items.md .planning/quick/260903-ceo-beispiel-bundle-statt-kundendaten/deferred-items.md .planning/quick/260903-da5-kleinigkeiten-aus-der-freigabepruefung/deferred-items.md</automated>
    <fails_when>any of the three files reports a count other than 1</fails_when>
    <automated>git diff --numstat -- .planning/quick/260902-cml-i18n-kataloge-sauber-0-offen-0-verwaist/deferred-items.md .planning/quick/260903-ceo-beispiel-bundle-statt-kundendaten/deferred-items.md .planning/quick/260903-da5-kleinigkeiten-aus-der-freigabepruefung/deferred-items.md</automated>
    <fails_when>any row shows a deletion count above 0 — the stamp replaced text instead of being added above it</fails_when>
  </verify>
  <done>All three findings read as closed on the first screen, with the reasoning a later phase needs preserved word for word and each closure confirmed against the tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning map → a future planner's decisions | a wrong number in `.planning/codebase/` becomes a wrong plan in Phases 7–10; nothing in the build catches it |
| research document → this task's edits | `06-RESEARCH.md` was measured at an older commit; copying from it without re-verification reintroduces drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-09 | Spoofing | a codebase map asserting a fact the tree does not support | medium | mitigate | Every corrected value is derived by running the command in the inventory's evidence column against current `HEAD`, and the command is recorded in the SUMMARY beside the value |
| T-06-10 | Tampering | regenerating a map instead of correcting it | medium | mitigate | D-19 forbids regeneration; the acceptance criteria cap `CONCERNS.md` at six changed lines and require the assessment finding to survive |
| T-06-11 | Repudiation | deleting a closed `deferred-items.md` instead of stamping it | low | mitigate | D-20's header stamp; acceptance requires zero deletions in `git diff --numstat` for all three files |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan; it edits Markdown only. `06-RESEARCH.md` §"Package Legitimacy Audit" records an empty table |
</threat_model>

<verification>
- `grep -rn '2026-08-22' .planning/codebase/` prints nothing.
- `grep -rn 'k8s/\|deploy\.yml\|Raspberry Pi\|linux/arm64' .planning/codebase/` prints nothing.
- `grep -n '^## [0-9]' docs/offene-punkte.md` lists no heading above 6.
- Each of the three `deferred-items.md` carries exactly one closure stamp and zero deleted lines.
- `git diff --name-only` for this plan lists only the eleven files in `files_modified`.
</verification>

<success_criteria>
- MAINT-05's pre-existing drift is gone: seven maps, one working list and three deferred notes read true against the working tree.
- Every changed number has a command recorded beside it that reproduces it.
- The three anchors this phase itself changes are untouched and left to plan 06-06.
</success_criteria>

<artifacts_this_phase_produces>
Changed by this plan (no new symbols, no new files):

| Kind | Name |
|------|------|
| Doc stamp | `Analysis Date` refreshed on all seven `.planning/codebase/*.md` |
| Doc line | a "corrected surgically, not regenerated" note under each map's stamp |
| Doc section | the Dependabot procedure relocated into `## Beim Weiterarbeiten` in `docs/offene-punkte.md` |
| Doc stamp | one `> **Erledigt am …**` blockquote at the head of each of the three `deferred-items.md` |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-aufr-umen/06-03-SUMMARY.md` when done.
</output>
