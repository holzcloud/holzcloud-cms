---
phase: 06-aufr-umen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [MAINT-03, MAINT-05]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "REQUIREMENTS.md MAINT-03 and MAINT-05, and ROADMAP.md Phase 6 success criteria 3 and 5, state what this phase will actually deliver — six committed .wasm files, four committed .zip archives, seven codebase maps — and no longer contradict 06-CONTEXT.md."
    - "ROADMAP.md Phase 6 planning notes name `go run ./tools/wasm` as the build shape, carry the GOTOOLCHAIN pin and mandatory `-buildvcs=false`, and their pointer into `tools/i18n/main.go` matches the line a grep reports."
    - "The amendment is one docs-only commit, and it is the first commit of the phase — no code file changed before it."
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  key_links:
    - "this plan → every other plan in the phase. `06-02` through `06-07` all declare it as a predecessor, so the wave graph enforces D-01's ordering rather than only describing it."
    - "`06-RESEARCH.md` §\"MAINT-05 Correction Inventory\" → the checklist the amended success criterion 5 points at, so the criterion stays checkable instead of carrying a count that is itself stale."
---

<objective>
Amend the two documents Phases 7–10 are planned against so neither contradicts the
decisions this phase executes — in its own docs commit, before any code is touched.

Purpose: D-01, locked. A phase whose entire purpose is correct ground truth must not
leave its own two source documents disagreeing with its context file. Four of the
amendments also make later plans checkable: a success criterion that names a stale
count cannot be verified, and a planning note that names a build script which never
existed sends the next reader to a file that is not there.

Output: an amended `REQUIREMENTS.md` and `ROADMAP.md` in a single docs-only commit.

**Why this is a plan of its own rather than the first task of another.** D-01 says
"before any code is touched", and that is an ordering claim about the whole phase,
not about one plan's internal task list. Expressing it as `wave: 1` with every other
plan declaring `depends_on: ["06-01"]` is what makes the wave graph carry it. As a
side effect the `git show … HEAD` assertion below inspects a commit this plan
certainly produced, because no sibling plan runs concurrently with it.

**Tracer note.** The phase still leads with a tracer: `06-02` Task 1 is the
`type="tracer"` slice, and it is the first code task in the phase. This plan changes
no code.
</objective>

<execution_context>
@/Users/holz/.claude/gsd-core/workflows/execute-plan.md
@/Users/holz/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-aufr-umen/06-CONTEXT.md
@.planning/phases/06-aufr-umen/06-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend REQUIREMENTS.md and ROADMAP.md to the locked decisions (D-01) — its own docs commit, before any code is touched</name>
  <read_first>
    - .planning/REQUIREMENTS.md — lines 113–117 (MAINT-01…05) and lines 218–226 (the traceability table)
    - .planning/ROADMAP.md — lines 93–116 (`### Phase 6: Aufräumen`: Goal, the six Success Criteria, Planning notes)
    - .planning/phases/06-aufr-umen/06-CONTEXT.md — D-01, D-02, D-02a, D-03, D-03a, D-06, D-07, D-18, D-23, and the **WITHDRAWN** D-17a block at lines 191–204
    - .planning/phases/06-aufr-umen/06-RESEARCH.md — §"MAINT-05 Correction Inventory" (the checklist the amended criterion 5 will point at), §"CI Cost (MAINT-03, D-08)" (the measured 4.6 s)
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Amend the two source documents so neither contradicts 06-CONTEXT.md. Make only the
edits listed here; leave every other requirement, phase and line untouched. Use
scoped replacements, never a whole-file rewrite.

In `.planning/REQUIREMENTS.md`:

(a) MAINT-03 at line 115 — replace the glob `plugins/*/plugin.wasm` with a phrase
covering all six committed modules: the five under `plugins/` plus
`internal/plugin/testdata/echo.wasm`. Per D-07 the glob does not literally reach
`echo.wasm` while MAINT-04's "five tests" does, and that asymmetry is the defect
D-01 exists to close. Name the rule the decision gives: no committed `.wasm` in
this repository may go stale unchecked.

(b) MAINT-05 at line 117 — replace the clause naming `.planning/codebase/ARCHITECTURE.md`
and its count of drifted facts with a clause covering **all seven** maps in
`.planning/codebase/`, and point at `06-RESEARCH.md` §"MAINT-05 Correction Inventory"
as the line-referenced checklist (D-18). State that every map carries an
`Analysis Date` from 2026-08-22, that `INTEGRATIONS.md` still documents a Kubernetes
directory, a deployment workflow and an arm64 cross-compile job that no longer exist,
and that the corrections are surgical and never regenerated (D-19). Keep the
`docs/offene-punkte.md` and `deferred-items.md` clauses as written.

Leave the traceability table rows at lines 220–224 alone; their Status column is
updated by phase completion, not by this task.

In `.planning/ROADMAP.md`, inside `### Phase 6: Aufräumen` only:

(c) Success criterion 3 — widen from `every plugins/*/plugin.wasm` to all six
committed modules including `internal/plugin/testdata/echo.wasm`, and add that the
four committed `.zip` archives beside the plugins are repacked by the same tool so
the rebuild cannot desynchronise them (D-23).

(d) Success criterion 5 — replace the clause naming six drifted facts in
`ARCHITECTURE.md` with one naming the drifted facts and file references across all
seven maps, listed in `06-RESEARCH.md` §"MAINT-05 Correction Inventory".

(e) Planning note, shape line — the note currently reads that the shape is
`plugins/build.sh` plus a CI step plus `HOLZCLOUD_TEST_REQUIRE_WASM=1`. Replace
`plugins/build.sh` with `tools/wasm`, a Go command invoked as `go run ./tools/wasm`,
per D-06: this repository's own tooling is Go commands under `tools/`, shell lives
only under `deploy/`, and `plugins/build.sh` never existed.

(f) Planning note, the i18n pointer — **do not change the number.** D-17a proposed
moving it and has since been **WITHDRAWN**: the roadmap's existing pointer is
correct and the proposed correction was itself the error. What survives from that
finding, and what the note should say plainly, is that the claim MAINT-02 removes
lives in the `writeCatalog` doc comment, not in the package doc comment at
`tools/i18n/main.go:1–15`, which makes no claim about output shape at all.

Still grep rather than trust: run `grep -n 'sorted and indented' tools/i18n/main.go`
and confirm the note's number equals what it reports (measured 287 on 2026-09-04).
If the grep disagrees, write what the grep says and record the disagreement in the
SUMMARY. Note for the reader of this plan, not for the file: plan `06-03` deletes
that claim, and plan `06-07` Task 3 retires this note once it has nothing left to
point at.

(g) New planning note, the toolchain pin (D-02, D-03, D-03a) — `tools/wasm` sets
`GOTOOLCHAIN` to an exact patch level as a constant inside the tool, not in CI's
`setup-go` and not as a `toolchain` line in `go.mod`. The pin has a floor: it may
not be lower than the `go` directive in the root `go.mod` (`go 1.26.6` today),
because `internal/plugin/testdata/echo` lives in the root module. Raising the `go`
directive therefore means raising the pin and rebuilding the six guests in the same
commit.

(h) New planning note, `-buildvcs=false` (D-02a) — mandatory, and the precondition
for the byte comparison rather than an optimisation. Every committed module today
carries `vcs.revision`, `vcs.time` and `vcs.modified=true` from the default
`-buildvcs=auto`; the revision is the git SHA at build time, so a CI rebuild happens
at a different commit by construction and can never match. State the four places
the flag must appear: `tools/wasm`, `plugins/README.md`,
`internal/plugin/testdata/README.md`, and the `//go:generate` line in
`internal/plugin/runtime_test.go`.

(i) The CI-budget planning note — it warns that adding five wasm builds has a cost
and asks the plan to budget it. Amend it with the measurement from
`06-RESEARCH.md` §"CI Cost": six wasip1 guests from a completely empty `GOCACHE`
cost about 4.6 s of wall time, and the 297 s figure recorded in `security.yml`
belongs to `go test -race ./internal/plugin/`, which this step does not touch.

Commit with a `docs(06):` message. No code file may be in the same commit, and this
must be the first commit of the phase.
  </action>
  <acceptance_criteria>
    <!-- planner-discipline-allow: plugins/*/plugin.wasm -->
    <!-- planner-discipline-allow: plugins/build.sh -->
    - `grep -c 'plugins/\*/plugin.wasm' .planning/REQUIREMENTS.md` reports 0, and the MAINT-03 line contains the substring `echo.wasm`.
    - The MAINT-05 line in `.planning/REQUIREMENTS.md` contains the substring `06-RESEARCH.md` and the substring `seven`.
    - `sed -n '/### Phase 6/,/### Phase 7/p' .planning/ROADMAP.md | grep -c 'plugins/build.sh'` reports 0.
    - `sed -n '/### Phase 6/,/### Phase 7/p' .planning/ROADMAP.md | grep -c -- '-buildvcs=false'` reports at least 1.
    - `sed -n '/### Phase 6/,/### Phase 7/p' .planning/ROADMAP.md | grep -c 'GOTOOLCHAIN'` reports at least 1.
    - `sed -n '/### Phase 6/,/### Phase 7/p' .planning/ROADMAP.md | grep -c 'tools/wasm'` reports at least 1.
    - The Phase 6 pointer into `tools/i18n/main.go` equals the line number reported by `grep -n 'sorted and indented' tools/i18n/main.go`, and the amended note says the claim is in the `writeCatalog` doc comment rather than the package doc comment.
    - `ls .planning/phases/06-aufr-umen/ | grep -c SUMMARY` reports 0 before this task commits — no sibling plan has run, which is what makes the next criterion meaningful.
    - `git show --stat --name-only --format= HEAD` lists exactly `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`.
    - `git diff HEAD~1 -- .planning/ROADMAP.md` shows no change to any line inside `### Phase 7`, `### Phase 8`, `### Phase 9` or `### Phase 10`.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'plugins/\*/plugin.wasm' .planning/REQUIREMENTS.md; sed -n '/### Phase 6/,/### Phase 7/p' .planning/ROADMAP.md | grep -c 'plugins/build.sh'</automated>
    <fails_when>either command prints a number other than 0</fails_when>
    <automated>grep -n 'sorted and indented' tools/i18n/main.go</automated>
    <fails_when>no output (the anchor moved and the pointer written into ROADMAP.md cannot be trusted), or more than one line is printed</fails_when>
    <automated>git show --stat --name-only --format= HEAD</automated>
    <fails_when>the listing names any path outside `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`, or names fewer than both</fails_when>
    <automated>ls .planning/phases/06-aufr-umen/ | grep -c 'SUMMARY'</automated>
    <fails_when>a count other than 0 — another plan of this phase already ran, so this amendment is not the phase's first commit and D-01's ordering was not honoured</fails_when>
  </verify>
  <done>Both source documents state what this phase delivers — six committed modules, four archives, seven codebase maps, `tools/wasm`, the `GOTOOLCHAIN` pin, mandatory `-buildvcs=false` and the measured CI cost — and the amendment is the phase's first commit, containing nothing but those two documents.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning document → a future planner's decisions | a success criterion carrying a stale count cannot be verified, and Phases 7–10 are planned against these two files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-00 | Spoofing | a requirement or success criterion asserting a scope the phase does not deliver | medium | mitigate | Every amended clause is derived from a locked decision in `06-CONTEXT.md`, and the i18n pointer is re-derived by grep rather than copied |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan; it edits Markdown only. `06-RESEARCH.md` §"Package Legitimacy Audit" records an empty table |
</threat_model>

<verification>
- `git show --stat --name-only --format= HEAD` lists exactly the two documents.
- No commit in this phase precedes it that touches a file outside those two documents.
- Every negative grep in the acceptance criteria reports 0 and every positive one reports at least 1.
</verification>

<success_criteria>
- `REQUIREMENTS.md` MAINT-03 covers all six committed modules and MAINT-05 covers all seven codebase maps.
- `ROADMAP.md` Phase 6 names `tools/wasm`, the `GOTOOLCHAIN` pin, mandatory `-buildvcs=false`, the measured 4.6 s CI cost, and a pointer into `tools/i18n/main.go` a grep confirms.
- The amendment is the phase's first commit and contains nothing else.
</success_criteria>

<artifacts_this_phase_produces>
Changed by this plan (no new symbols, no new files):

| Kind | Name |
|------|------|
| Requirement text | `MAINT-03` and `MAINT-05` in `.planning/REQUIREMENTS.md` |
| Success criterion | Phase 6 criteria 3 and 5 in `.planning/ROADMAP.md` |
| Planning note | the build-shape note, now naming `go run ./tools/wasm` |
| Planning note | the `GOTOOLCHAIN` pin note (new) |
| Planning note | the mandatory `-buildvcs=false` note (new), naming its four locations |
| Planning note | the CI-budget note, now carrying the measured 4.6 s |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-aufr-umen/06-01-SUMMARY.md` when done.
</output>
