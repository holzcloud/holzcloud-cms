---
phase: quick-260906-m9z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/holzcloud/templates/admin/field_list.html          # Zeilen 8, 16, 25 — drei Zeichen, sonst nichts
  - .planning/WINDOWS.md                                   # Eintrag 5 geschlossen, Begruendung berichtigt
  - .planning/phases/08-snippets-carry-fields/deferred-items.md  # derselbe Befund, datierter Stempel
autonomous: true
requirements: [QUICK-260906-m9z, QUAL-01, QUAL-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Ueber dem Feldbildschirm steht auf allen drei Ruecklegen ein Pfeilzeichen und nicht mehr der Text der HTML-Entitaet — sichtbar in der laufenden Anwendung, nicht nur im Quelltext."
    - "`go run ./tools/i18n` meldet weiterhin genau `1158 Zeichenketten im Quelltext` und fuer en/es/fr/it je `1158 uebersetzt, 0 offen, 0 verwaist`. Die Zahl bewegt sich NICHT — bewegt sie sich, hat sich der Schluessel geaendert und der Flick ist falsch."
    - "Kein Katalog wurde angefasst: `git status` nennt keine Datei unter `internal/i18n/locales/`."
    - "Wer `.planning/WINDOWS.md` Eintrag 5 liest, findet ihn geschlossen UND findet dort stehen, dass die Begruendung des Aufschubs auf einer falschen Annahme ruhte — der alte Text ist nicht geloescht, er ist berichtigt."
    - "Wer die Schrittfolge in `deferred-items.md` liest, wird nicht mehr zu dem Flick verleitet, der die Schluessel tatsaechlich verwaisen wuerde."
  artifacts:
    - "cmd/holzcloud/templates/admin/field_list.html — drei Aufrufstellen auf die HTML-durchlassende Fassung umgestellt"
    - ".planning/WINDOWS.md — Eintrag 5 mit status fixed, resolved_at gesetzt, Beschreibung um die Berichtigung ergaenzt"
    - ".planning/phases/08-snippets-carry-fields/deferred-items.md — datierter Stempel ueber dem Abschnitt aus Plan 08-05"
  key_links:
    - "Die Zeichenkette IST der Katalogschluessel. `tools/i18n/main.go:47` sammelt `t`, `th` und `tf` mit EINEM Ausdruck — solange die Zeichenkette byte-gleich bleibt, sieht der Zaehler denselben Schluessel und keine Uebersetzung verwaist. Genau das prueft das 1158-Tor."
    - "`internal/web/render.go:90` definiert `th` als `template.HTML(i18n.T(lang, s))`. `internal/web/render_test.go:19` zerlegt das echte Verzeichnis `cmd/holzcloud/templates/admin` — `go test ./internal/web/...` ist damit das Tor darauf, dass die Vorlage nach dem Flick ueberhaupt noch zerlegbar ist."
---

<objective>
Drei Zeichen in einer Vorlage, und zwei Akteneintraege, die eine falsche
Begruendung tragen.

`cmd/holzcloud/templates/admin/field_list.html` druckt auf den Zeilen 8, 16 und
25 den Text `&#8592; Alle Bausteinarten` / `&#8592; Alle Textbausteine` /
`&#8592; Alle Felder`, weil die uebersetzende Funktion ihr Ergebnis maskiert und
das `&` der Entitaet dabei selbst zu `&amp;` wird. Im Browser gesehen
(Nachdurchgang zu Phase 8, Aufnahme `01b`), nicht aus dem Quelltext gefolgert.

**Der Aufschub ruhte auf einer falschen Annahme.** Aufgeschoben wurde mit der
Begruendung, die Zeichenkette sei der Katalogschluessel und ein Flick verwaise
drei Schluessel in vier Katalogen. Das gilt fuer den Flick, den
`deferred-items.md` vorschlaegt — die Entitaet durch das Zeichen `←` zu
ersetzen. Es gilt NICHT fuer den Flick, der hier gefahren wird: die Zeichenkette
bleibt byte-fuer-byte dieselbe, nur die aufrufende Funktion wechselt auf die
Fassung, die HTML durchlaesst. Kein Schluessel verwaist, kein Katalog wird
angefasst, das Tor `0 verwaist` bleibt stehen.

Purpose: der Rueckweg zeigt einen Pfeil, und die Akte sagt die Wahrheit
darueber, warum das ein Jahr lang wie eine grosse Arbeit aussah.
Output: eine Vorlage mit drei geaenderten Zeichen, zwei geschlossene
Akteneintraege mit berichtigter Begruendung, zwei Commits.

**Kein Tracer.** Die Aenderung beruehrt genau eine Schicht und genau eine Datei;
sie IST bereits die duennste moegliche Scheibe. Ein vorgeschalteter
Tracer-Task waere hier reine Zeremonie.

**QUAL-02 gehoert nicht dem Executor.** Der Pfeil muss in der laufenden
Anwendung gesehen werden, bevor die Aufgabe zaehlt — diesen Durchgang faehrt der
**Orchestrator** nach der Ausfuegung mit dem Playwright-MCP-Server. Der Executor
baut keinen Browserschritt ein und gibt keine Checkliste zurueck.
</objective>

<execution_context>
@/Users/holz/.claude/gsd-core/workflows/execute-plan.md
@/Users/holz/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/WINDOWS.md
@.planning/phases/08-snippets-carry-fields/deferred-items.md
@cmd/holzcloud/templates/admin/field_list.html

Gemessener Ausgangszustand (am 2026-09-06 gegen HEAD `da6c2fb` gefahren, nicht
geschaetzt):

- `go run ./tools/i18n` sagt `1158 Zeichenketten im Quelltext`; en/es/fr/it je
  `1158 uebersetzt, 0 offen, 0 verwaist`. de-CH 55 Abweichungen, fr-CH 4,
  it-CH 9 — alle drei `0 ohne Gegenstueck`.
- Die drei Aufrufstellen sind ausschliesslich in dieser einen Datei:
  `grep -rn` ueber `cmd/holzcloud/templates/` findet 3 Treffer, alle in
  `field_list.html`, auf 8, 16 und 25. Es gibt nichts weiter umzustellen.
- Das Muster steht schon im Baum: `website_list.html:5` und `:7` reichen
  `<strong>`/`<em>` innerhalb einer Katalogzeichenkette bereits durch dieselbe
  HTML-durchlassende Fassung.
- **Der Arbeitsbaum ist nicht leer, und was darin liegt, wechselt.** Beim
  Schreiben dieses Plans lagen nacheinander eine geaenderte
  `.planning/config.json`, eine unversionierte `.planning/milestone.lock` und
  zuletzt ein 33-MB-Bauergebnis `./holzcloud` darin (Zeitstempel 15:42, aelter
  als dieser Auftrag, **nicht** von `.gitignore` gedeckt). Nichts davon gehoert
  in diese Commits, und die Liste ist nicht abschliessend. Deshalb die Regel
  statt der Liste: es wird ausschliesslich mit **benannten Dateien** committet,
  nie mit `-a`, und vor jedem Commit sagt `git status --porcelain`, was
  tatsaechlich im Baum liegt. Das Bauergebnis wird weder committet noch
  geloescht — es gehoert diesem Auftrag nicht.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Der Rueckweg bekommt seinen Pfeil</name>
  <files>cmd/holzcloud/templates/admin/field_list.html</files>
  <action>
    In `cmd/holzcloud/templates/admin/field_list.html` auf den Zeilen 8, 16 und
    25 den Namen der uebersetzenden Funktion innerhalb der Template-Aktion von
    der maskierenden Fassung auf die HTML-durchlassende Fassung `th` setzen.
    Nach der Aenderung lautet der Aufruf auf jeder der drei Zeilen
    `{{th "&#8592; Alle Bausteinarten"}}` beziehungsweise
    `{{th "&#8592; Alle Textbausteine"}}` beziehungsweise
    `{{th "&#8592; Alle Felder"}}`.

    Was NICHT angefasst wird, und das ist der ganze Kern der Sache: die
    Zeichenkette hinter dem Funktionsnamen bleibt byte-fuer-byte dieselbe. Die
    Entitaet wird NICHT durch das Zeichen `←` ersetzt — das waere die
    Aenderung, die drei Katalogschluessel verwaisen liesse, und sie ist hier
    ausdruecklich verboten. Kein Anfuehrungszeichen, kein Leerzeichen, kein
    Semikolon in diesen Zeichenketten wird beruehrt. Auch das `href` und das
    umgebende Markup bleiben unveraendert.

    Keine weitere Datei wird geoeffnet: keine andere Vorlage, keine Datei unter
    `internal/i18n/locales/`, kein Go-Quelltext. `go run ./tools/i18n -write`
    wird NICHT gefahren — es gibt nichts zu schreiben, und das Tor in
    `<verify>` beweist genau das.

    In die Vorlage wird KEIN Kommentar eingefuegt. Die Begruendung gehoert in
    die Commit-Nachricht, nicht ins Markup.

    Danach committen — nur diese eine Datei, benannt, nie `-a`. Nachricht auf
    Deutsch im Register des Repositoriums, Betreff
    `fix(quick-260906-m9z-01): der Rueckweg ueber dem Feldbildschirm zeigt endlich einen Pfeil`.
    Der Rumpf muss zwei Dinge sagen: erstens, dass die Zeichenkette unveraendert
    bleibt und deshalb kein Katalogschluessel verwaist (mit der gemessenen Zahl
    1158 als Beleg); zweitens die Sicherheitsaussage — der Inhalt kommt aus
    einer Katalogdatei im Repositorium und nie aus einer Benutzereingabe, der
    `template.HTML`-Guss eroeffnet also keine Einschleusungsflaeche.
  </action>
  <verify>
    <automated>
    cd /Users/holz/Projects/holzcloud-cms && \
    echo "-- Tor 3a: die drei neuen Aufrufstellen, erwartet 3 --" && \
    grep -c '{{th "&#8592;' cmd/holzcloud/templates/admin/field_list.html && \
    echo "-- Tor 3b: keine maskierende Aufrufstelle mehr, erwartet 0 --" && \
    grep -c '{{t "&#8592;' cmd/holzcloud/templates/admin/field_list.html; \
    echo "-- Tor 1: der Zaehler darf sich NICHT bewegen --" && \
    go run ./tools/i18n && \
    echo "-- Gegenprobe: kein Katalog angefasst, erwartet keine Ausgabe --" && \
    git status --porcelain internal/i18n/locales/ && \
    echo "-- Tor 2a: die Vorlage ist noch zerlegbar (render_test.go:19 liest das echte Verzeichnis) --" && \
    go test ./internal/web/... && \
    echo "-- Tor 2b: die ganze Reihe --" && \
    go test ./... && \
    echo "-- Tor 2c: gofmt, erwartet keine Ausgabe --" && \
    gofmt -l . && \
    echo "-- Tor 2d: go vet --" && \
    go vet ./...
    </automated>
    <expected>
    Tor 3a misst genau `3`. Tor 3b misst genau `0` — `grep -c` liefert dabei
    Rueckgabecode 1, das ist kein Fehlschlag, deshalb steht dort ein `;` und
    kein `&&`.
    Tor 1 gibt WOERTLICH `1158 Zeichenketten im Quelltext` aus und fuer
    `en.json`, `es.json`, `fr.json` und `it.json` je
    `1158 uebersetzt, 0 offen, 0 verwaist`. **Eine andere Zahl als 1158, oder
    ein `verwaist` groesser als 0, bedeutet: der Schluessel hat sich bewegt, der
    Flick ist falsch — anhalten, zuruecknehmen, melden.** Nicht durch
    Katalogpflege reparieren.
    Die Gegenprobe gibt nichts aus.
    Tor 2a, 2b: `ok`/`no test files` fuer jedes Paket, **0 Fehlschlaege**.
    Tor 2c gibt nichts aus. Tor 2d gibt nichts aus.
    </expected>
  </verify>
  <done>
    `field_list.html` misst 3 durchlassende und 0 maskierende Aufrufstellen fuer
    diese Zeichenketten; `go run ./tools/i18n` steht unveraendert auf 1158 mit
    `0 offen, 0 verwaist` fuer alle vier Vollkataloge; unter
    `internal/i18n/locales/` ist nichts geaendert; `go test ./...` ohne
    Fehlschlag, `gofmt -l .` und `go vet ./...` stumm; ein Commit, der nur
    `field_list.html` enthaelt und dessen Rumpf die Schluessel- und die
    Sicherheitsaussage traegt.

    **Noch nicht erledigt im Sinne von QUAL-02:** der Pfeil muss in der
    laufenden Anwendung gesehen worden sein. Das faehrt der Orchestrator nach
    der Ausfuegung; der Executor faehrt keinen Browser und gibt keine
    Checkliste zurueck.
  </done>
</task>

<task type="auto">
  <name>Task 2: Die Akte sagt, warum die Begruendung falsch war</name>
  <files>.planning/WINDOWS.md, .planning/phases/08-snippets-carry-fields/deferred-items.md</files>
  <action>
    Zwei Eintraege schliessen — und in beiden die berichtigte Begruendung
    hinschreiben statt sie still zu loeschen. Die Konvention aus Phase 06 gilt:
    ein geschlossener Befund wird durch einen datierten Stempel ueber seinem
    Text geschlossen, nicht durch Loeschen; die Begruendung ist das, was eine
    spaetere Phase braucht.

    **A — `.planning/WINDOWS.md`, Eintrag 5.** Die kanonische Fassung ist der
    JSON-Block am Dateiende; die Markdown-Tabelle darueber wird daraus
    gezeichnet. Deshalb in dieser Reihenfolge arbeiten, sonst laufen die beiden
    Flaechen auseinander:

    Erstens im JSON-Block das Feld `description` des Eintrags mit `"id": 5`
    ergaenzen — der bestehende Satz bleibt vollstaendig stehen, angehaengt wird
    die Berichtigung. Sie muss sagen: die Begruendung des Aufschubs
    (`Aenderung verwaist drei Katalogschluessel`) ruhte auf einer falschen
    Annahme darueber, worin der Flick besteht; der tatsaechliche Flick wechselt
    an denselben drei Stellen nur die aufrufende Funktion auf die
    HTML-durchlassende Fassung, die Zeichenkette bleibt byte-gleich, kein
    Schluessel verwaist, der Zaehler stand vorher wie nachher auf 1158;
    geschlossen im Schnellauftrag 260906-m9z am 2026-09-06.
    Zwei harte Formregeln, weil die Tabelle aus diesem Feld gezeichnet wird:
    der angehaengte Text enthaelt **keinen senkrechten Strich** und **keine
    Zeilenschaltung**, und die Datei bleibt gueltiges JSON. Die Feldnamen
    `status`, `resolved_at` und die Zaehler im Kopf werden VON HAND NICHT
    angefasst.

    Zweitens `windows fixed 5` ueber das GSD-Werkzeug fahren (siehe `<verify>`).
    Es liest den berichtigten JSON-Block, setzt `status` auf `fixed`, setzt
    `resolved_at`, zieht die Zaehler im Kopf nach und zeichnet die
    Markdown-Tabelle neu — dadurch traegt auch die Tabellenzeile die
    berichtigte Beschreibung, ohne dass sie ein zweites Mal von Hand
    geschrieben wird. Das Werkzeug verlangt in diesem Repositorium
    `--project-dir`; ohne den Schalter loest es die Wurzel nicht auf.

    **B — `.planning/phases/08-snippets-carry-fields/deferred-items.md`.** Ueber
    den Abschnitt aus Plan 08-05, der mit der Feststellung zu `field_list.html`
    beginnt, einen datierten Stempel setzen: erledigt am 2026-09-06 im
    Schnellauftrag 260906-m9z, mit der berichtigten Begruendung in denselben
    Worten wie in A. Der bestehende Text bleibt stehen — er ist der Beleg
    dafuer, was damals geglaubt wurde.

    Eine Stelle darf jedoch nicht unmarkiert stehen bleiben: der Absatz, der mit
    `Was zu tun ist` beginnt, schreibt die Schrittfolge fuer den FALSCHEN Flick
    vor (Entitaet durch das Zeichen ersetzen, `-write` fahren, drei Schluessel
    neu uebersetzen, drei alte aus vier Katalogen streichen). Diesen Absatz
    ausdruecklich als ueberholt kennzeichnen und dazuschreiben, dass genau diese
    Schrittfolge die Verwaisung erst erzeugt haette, die zu vermeiden das Ziel
    war. Geloescht wird er nicht. Die uebrigen Abschnitte dieser Datei — der
    aus Plan 08-04 und die drei aus dem Code-Review — bleiben unangetastet.

    Danach committen, nur die beiden benannten Dateien, nie `-a`, Betreff
    `docs(quick-260906-m9z-01): der Aufschub ruhte auf einer falschen Annahme, und das gehoert auf die Akte`.

    **Zwei Commits, nicht einer — die Entscheidung und ihr Grund:** der Flick
    und die Berichtigung der Akte reisen getrennt, weil der Flick fuer sich
    zuruecknehmbar bleiben muss, ohne die berichtigte Aktenlage mitzureissen.
    Die Feststellung „die Begruendung war falsch" bleibt wahr, auch wenn der
    Flick je zurueckgenommen wuerde.
  </action>
  <verify>
    <automated>
    cd /Users/holz/Projects/holzcloud-cms && \
    echo "-- Eintrag 5 auf fixed setzen --" && \
    node /Users/holz/.claude/gsd-core/bin/gsd-tools.cjs windows fixed 5 --project-dir /Users/holz/Projects/holzcloud-cms && \
    echo "-- offene Fenster, vorher 3, erwartet 2 --" && \
    node /Users/holz/.claude/gsd-core/bin/gsd-tools.cjs windows status --project-dir /Users/holz/Projects/holzcloud-cms --pick ledger.open_count && \
    echo "-- Status des Eintrags 5, erwartet fixed --" && \
    node /Users/holz/.claude/gsd-core/bin/gsd-tools.cjs windows status --project-dir /Users/holz/Projects/holzcloud-cms --pick 'ledger.entries[4].status' && \
    echo "-- die Berichtigung steht in der Beschreibung, erwartet 1 --" && \
    node /Users/holz/.claude/gsd-core/bin/gsd-tools.cjs windows status --project-dir /Users/holz/Projects/holzcloud-cms --pick 'ledger.entries[4].description' | grep -c '260906-m9z' && \
    echo "-- die Akte traegt den Stempel, erwartet mindestens 1 --" && \
    grep -c '260906-m9z' .planning/phases/08-snippets-carry-fields/deferred-items.md && \
    echo "-- die falsche Schrittfolge ist als ueberholt markiert, erwartet mindestens 1 --" && \
    grep -ci 'ueberholt\|überholt' .planning/phases/08-snippets-carry-fields/deferred-items.md && \
    echo "-- der alte Text ist NICHT geloescht, erwartet 1 --" && \
    grep -c 'ist der Katalogschlüssel' .planning/phases/08-snippets-carry-fields/deferred-items.md && \
    echo "-- die Akte des Code-Reviews ist unangetastet, erwartet 1 --" && \
    grep -c 'WR-03' .planning/phases/08-snippets-carry-fields/deferred-items.md
    </automated>
    <expected>
    `windows fixed 5` meldet Erfolg und faellt nicht mit
    `windows_ledger_malformed` — faellt es so, ist die Handaenderung am
    JSON-Block ungueltiges JSON oder hat die Eintragszahl bewegt; dann die
    Aenderung zuruecknehmen und noch einmal ansetzen.
    `open_count` misst `2`. Der Status des fuenften Eintrags liest `fixed`.
    Die drei `grep -c` auf die Beschreibung, den Stempel und die
    Ueberholt-Markierung messen je mindestens `1`. Der alte Satz ueber den
    Katalogschluessel misst `1` — er steht noch da. `WR-03` misst `1`.
    </expected>
  </verify>
  <done>
    `.planning/WINDOWS.md` fuehrt Eintrag 5 als `fixed` mit gesetztem
    `resolved_at`, `open_count` steht auf 2, und die Beschreibung des Eintrags
    nennt sowohl die alte falsche Begruendung als auch die Berichtigung samt
    Schnellauftragsnummer. `deferred-items.md` traegt ueber dem Abschnitt aus
    Plan 08-05 den datierten Stempel, der alte Text steht unveraendert darunter,
    und der Absatz mit der falschen Schrittfolge ist als ueberholt
    gekennzeichnet. Ein zweiter Commit mit genau diesen beiden Dateien.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Katalogdatei → Browser-HTML | Die uebersetzte Zeichenkette wird an den drei Stellen kuenftig als `template.HTML` ausgegeben und nicht mehr maskiert. |
| Arbeitsbaum → Commit | Der Baum traegt fremde ungetrackte und geaenderte Dateien, die nicht mitreisen duerfen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-M9Z-01 | Tampering | `internal/web/render.go:90` (`th`), drei Aufrufstellen in `field_list.html` | low | accept | Der `template.HTML`-Guss maskiert nicht. Die Eingabe ist hier ausschliesslich ein Zeichenkettenliteral aus einer in das Binaerprogramm eingebetteten Katalogdatei im Repositorium — nie eine Benutzereingabe, nie ein Datenbankwert, nie ein Anfrageparameter. Genau das sagt der Kommentar bei `render.go:84-89` bereits als Bedingung dieser Funktion, und `website_list.html:5`/`:7` nutzen sie unter derselben Bedingung. Angenommen, mit dieser Begruendung in der Commit-Nachricht. |
| T-M9Z-02 | Tampering | i18n-Kataloge unter `internal/i18n/locales/` | medium | mitigate | Ein Flick, der die Zeichenkette antastet, verwaist drei Schluessel in vier Katalogen. Massnahme: der Task verbietet die Zeichenkettenaenderung ausdruecklich, das 1158-Tor misst sie, und `git status internal/i18n/locales/` weist nach, dass kein Katalog angefasst wurde. |
| T-M9Z-03 | Tampering | Commit-Inhalt (`.planning/config.json`, `.planning/milestone.lock`) | low | mitigate | Beide Tasks committen ausschliesslich benannte Dateien; `git commit -a` ist verboten. |
| T-M9Z-SC | Tampering | npm/pip/cargo installs | n/a | accept | Dieser Plan installiert kein Paket und beruehrt weder `go.mod` noch `go.sum`. Die Paket-Echtheitspruefung ist nicht anwendbar; entsteht wider Erwarten ein Installationsschritt, ist der Plan verlassen worden. |
</threat_model>

<verification>
Gefahren wird, was oben in `<verify>` steht — die Ausgaben werden **berichtet,
nicht behauptet**. Drei Tore tragen die Aufgabe:

1. **Der Zaehler bewegt sich nicht.** `go run ./tools/i18n` → `1158
   Zeichenketten im Quelltext`, und `0 offen, 0 verwaist` fuer en/es/fr/it.
   Bewegt er sich, ist der Flick falsch.
2. **Der Baum bleibt gruen.** `go test ./...` ohne Fehlschlag, `gofmt -l .`
   stumm, `go vet ./...` stumm. `go test ./internal/web/...` ist dabei der Teil,
   der wirklich etwas ueber diese Aenderung sagt: `render_test.go:19` zerlegt
   das echte Vorlagenverzeichnis.
3. **Die Zaehlung stimmt.** 3 durchlassende, 0 maskierende Aufrufstellen in
   `field_list.html`.

**Was der Executor NICHT tut:** keinen Browser fahren, keine Katalogdatei
oeffnen, `-write` nicht ausfuehren, keine andere Vorlage anfassen, keine
Checkliste zurueckgeben.

**Was der Orchestrator danach tut (QUAL-02):** die Anwendung starten und die
drei Rueckwege ueber dem Feldbildschirm im Browser ansehen — Feldliste einer
Website, dieselbe mit `?baustein=`, dieselbe mit `?textbaustein=`. Erst wenn
dort ein Pfeilzeichen steht und nicht der Text der Entitaet, zaehlt die Aufgabe
als erledigt.
</verification>

<success_criteria>
- `grep -c '{{th "&#8592;' cmd/holzcloud/templates/admin/field_list.html` = **3**
- `grep -c '{{t "&#8592;' cmd/holzcloud/templates/admin/field_list.html` = **0**
- `go run ./tools/i18n` = **1158 Zeichenketten im Quelltext**, en/es/fr/it je **0 offen, 0 verwaist**
- `git status --porcelain internal/i18n/locales/` = leer
- `go test ./...` = **0 Fehlschlaege**; `gofmt -l .` leer; `go vet ./...` leer
- `windows status --pick ledger.open_count` = **2** (vorher 3), Eintrag 5 = `fixed`
- Eintrag 5 und der Abschnitt in `deferred-items.md` tragen beide die
  berichtigte Begruendung, und der alte Text steht noch da
- Zwei Commits, keine fremde Datei darin
- **Danach, durch den Orchestrator:** der Pfeil ist in der laufenden Anwendung
  gesehen worden
</success_criteria>

<output>
Create `.planning/quick/260906-m9z-pfeil-ruecklinks-t-zu-th-in-field-list-h/260906-m9z-SUMMARY.md` when done.

Die Zusammenfassung muss die **tatsaechliche Ausgabe** der drei Tore zitieren —
die Zeile mit der 1158, die Zahlen der beiden `grep -c` und das Ergebnis von
`go test ./...`. Eine Behauptung ohne die Ausgabe daneben genuegt hier nicht,
weil die 1158 der einzige Beweis dafuer ist, dass der Katalogschluessel
unangetastet blieb.
</output>
