---
phase: quick-260903-t0s
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/holzcloud/templates/public/rudel/style.css        # ersetzt, 2343 Zeilen -> neue gruene Fassung
  - cmd/holzcloud/templates/public/rudel/layout.html      # ersetzt
  - cmd/holzcloud/templates/public/rudel/page.html        # ersetzt
  - cmd/holzcloud/templates/public/rudel/home.html        # ersetzt
  - cmd/holzcloud/templates/public/rudel/list.html        # ersetzt
  - cmd/holzcloud/templates/public/rudel/search.html      # ersetzt
  - cmd/holzcloud/templates/public/rudel/gate.html        # ersetzt
  - cmd/holzcloud/templates/public/rudel/404.html         # ersetzt
  - cmd/holzcloud/templates/public/rudel/maintenance.html # ersetzt
  - cmd/holzcloud/templates/public/rudel/shop.html        # ersetzt
  - cmd/holzcloud/templates/public/rudel/product.html     # ersetzt
  - cmd/holzcloud/templates/public/rudel/cart.html        # ersetzt
  - cmd/holzcloud/templates/public/rudel/checkout.html    # ersetzt
  - cmd/holzcloud/templates/public/rudel/order.html       # ersetzt
  - cmd/holzcloud/templates/public/rudel/favicon.svg      # neu
  - cmd/holzcloud/templates/public/rudel/fonts/manrope-latin.woff2      # neu, Kopie aus weide/fonts/
  - cmd/holzcloud/templates/public/rudel/fonts/manrope-latin-ext.woff2  # neu, Kopie aus weide/fonts/
  - cmd/holzcloud/assets/VENDOR.md                        # Schriften-Abschnitt: dritter Ort
autonomous: true
requirements: [QUICK-260903-t0s]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Eine Besucherin oeffnet eine Seite der Vorlage `rudel` und sieht Waldgruen auf gebrochenem Weiss und Manrope — keine System-Serifen, keinen Cremegrund, keine schmale Spalte (D-07, D-04)."
    - "`holzcloud template check cmd/holzcloud/templates/public/rudel` meldet „no problems found\" — dasselbe Urteil, das ein Upload gaebe."
    - "Auf einem Telefon steht das Hauptmenue als SPALTE hinter einem Umschalter, nicht als Treppenmuster ueber drei Einzuege; oberhalb der Schwelle gibt es den Umschalter nicht (D-05)."
    - "Ein Bild allein in seinem Absatz spannt bis zum breiten Rand und ist in der Hoehe begrenzt — eine Seite aus reinem Markdown laesst keine 60 % der Breite leer, und ein Hochformat fuellt nicht anderthalb Bildschirme (D-06)."
    - "Eine Galerie und eine Kartenreihe zeigen ein hochformatiges Hundefoto brauchbar, und die Bildmitte, die der Kern je Bild mitgibt, wirkt weiterhin (D-10)."
    - "Kein Byte kommt von einem fremden Ursprung: Manrope liegt in fonts/ der Vorlage und wird ueber /t/fonts/ ausgeliefert (D-04, D-15)."
    - "Alle zwoelf Ansichten sind erhalten und in die neue Sprache uebersetzt; keine ist verschwunden (D-12)."
    - "Die schwaechste Tinte erfuellt AA fuer die kleinste Schriftgroesse — auf diesem Papier gemessen, nicht aus weide oder holzcloud uebernommen (D-08)."
    - "Ein in der Verwaltung eingestellter Regler gewinnt gegen die Vorlage, und eine eingestellte Rundung zieht die ganze Radienfamilie mit (D-02, D-09, D-16)."
    - "Die neun Bausteinarten sind ueber die Bruecke gekleidet, nicht einzeln nachgebaut (D-03)."
    - "Der Fuss zeigt `footer-kontakt` und `schwesterseite`, und der Verweis auf den Seehof liest sich als Satz und nicht als Kleingedrucktes (D-11)."
  artifacts:
    - cmd/holzcloud/templates/public/rudel/style.css
    - cmd/holzcloud/templates/public/rudel/layout.html
    - cmd/holzcloud/templates/public/rudel/page.html
    - cmd/holzcloud/templates/public/rudel/home.html
    - cmd/holzcloud/templates/public/rudel/favicon.svg
    - cmd/holzcloud/templates/public/rudel/fonts/manrope-latin.woff2
    - cmd/holzcloud/templates/public/rudel/fonts/manrope-latin-ext.woff2
    - cmd/holzcloud/assets/VENDOR.md
  key_links:
    - "@font-face -> /t/fonts/manrope-latin.woff2 -> internal/web/asset.go setzt font/woff2, nosniff und einen Jahres-Cache"
    - "internal/design/tokens.go -> <style> nach /t/style.css -> die vier Brueckenzeilen der CMS-Schicht (D-02)"
    - "cmd/holzcloud/assets/bausteine.css Zeilen 19..25 liest --ink/--paper/--brand/--line/--space-3/--space-4/--radius -> zehn Brueckenzeilen kleiden alle neun Bausteinarten (D-03)"
    - "internal/block/render.go:367..371 schreibt je Kachelbild ein style-Attribut mit dem Bildmittelpunkt -> die Vorlage muss den Zuschnitt behalten, damit die Angabe wirkt (D-10)"
    - "internal/template/render_test.go:195 TestShippedThemesRenderEveryView kennt `rudel` bereits; render_test.go:297 verlangt von jeder mitgelieferten Vorlage ein @media print (D-14)"
---

<objective>
Die mitgelieferte oeffentliche Vorlage `rudel` wird neu gestaltet: als gruene
Schwester der soeben fertiggestellten hellen Vorlage `weide`. Sie kleidet die
Website der Hundezucht Delnahida (Loewchen und Perro de Agua Espanol).

Purpose: Die heutige Fassung sieht aus wie eine Broschuere von 2009 —
System-Serifen, Cremegrund, Haarlinien, schmale Spalte. Die Architektur, die
`weide` traegt, ist gebaut, geprueft und im Browser gegen die echte Website
abgenommen. Sie wird hier ins Waldgruene uebersetzt, nicht neu erfunden — und
die zwei Befunde, die `weide` je einen Nachbesserungs-Commit gekostet haben,
stehen hier im ERSTEN Wurf drin.

Output: 15 Dateien in `cmd/holzcloud/templates/public/rudel/` (zwoelf
Ansichten, `layout.html`, `style.css`, `favicon.svg`) plus zwei aus
`weide/fonts/` kopierte woff2 in `fonts/`, dazu eine Ergaenzung im
Schriften-Abschnitt von VENDOR.md. Kein Go-Code aendert sich: `rudel` steht
bereits in `BuiltinTemplates` (internal/template/loader.go:518) und bereits in
der Testreihe (internal/template/render_test.go:195).

## Die Entscheidungen des Anwenders

Diese Nummern werden in den Aufgaben zitiert. Sie sind nicht verhandelbar.

- **D-01** Die Referenz ist ab jetzt `weide`, NICHT `holzcloud`. Die
  Architektur wird uebernommen: vier `@layer`, darunter die bewusst
  schichtlose CMS-Schicht, danach im `<style>` die Werte der Website. Drei
  Stufen, und die Reihenfolge ist die ganze Mechanik.
- **D-02** Bruecke zu den sechs Reglernamen aus `internal/design/tokens.go`,
  jede Zeile mit dem Entwurfswert als Rueckfall. Richtung immer vom Regler zum
  System, nie zurueck.
- **D-03** Bruecke zu den zehn generischen Namen, ueber die `bausteine.css`
  alle neun Bausteinarten kleidet. Nur wo daraus etwas Falsches folgt, kommt
  eine gezielte Regel dazu.
- **D-04** Manrope als woff2 in `fonts/` der Vorlage, ausgeliefert ueber
  `/t/fonts/`. Die beiden Dateien werden aus `weide/fonts/` KOPIERT — nichts
  wird heruntergeladen. Keine Schrift fester Breite.
- **D-05** Die Navigation unter der schmalen Schwelle muss eine SPALTE sein.
  Beide Menuebenen ausdruecklich in Spaltenrichtung und auf volle Breite
  gestreckt, jedes `<li>` volle Breite. Dazu der Checkbox-Umschalter; oberhalb
  der Schwelle Checkbox UND Beschriftung ausgeblendet, damit beide aus dem
  Tabulatorweg heraus sind; der Sprunglink bleibt erstes Kind von `<body>`.
  Delnahida hat SECHS Hauptpunkte, zwei davon mit Untermenue.
- **D-06** Ein Bild allein in seinem Absatz spannt bis zum breiten Rand, samt
  `figure:not(.hc-block)`, natuerlicher Breite und Hoehengrenze. Fuer diese
  Website ist die Hoehengrenze die tragende Regel, keine Vorsichtsmassnahme:
  85 Bilder, viele davon Hochformat.
- **D-07** Farbwelt Waldgruen. Marke `#325737` (der Wert der echten Website und
  zugleich der Entwurfswert), Papier ein Gebrochenweiss mit leicht kuehlerem,
  gruenlichem Zug (`#F7F6F0`), Tinte ein sehr dunkles Gruenschwarz (`#1B211A`).
  Drei Farbschleier im Grund: Gruen, Moos, warmer Sand.
- **D-08** Kontrast neu rechnen, nicht abschreiben. Die gemessenen
  Verhaeltnisse stehen im Plan UND im Kommentar der Datei. Die Marke wird in
  beiden Rollen geprueft: als Verweisfarbe und als Knopffuellung mit heller
  Schrift.
- **D-09** Rundung: das Manifest dieser Website setzt 10, das der
  Milchschaeferei 0. `rudel` ist also die rundere der beiden Vorlagen; die
  Entwurfswerte sollen das widerspiegeln, statt `weide` zu spiegeln.
- **D-10** Das Raster traegt Hochformat. `hc-galerie` und `hc-karten` werden
  ausdruecklich auf Hochformat geprueft; die Angabe zur Bildmitte, die
  `internal/block/render.go` je Kachelbild mitgibt, darf die Vorlage nicht
  wegwerfen.
- **D-11** Der Fussbereich traegt zwei Textbausteine, `footer-kontakt` und
  `schwesterseite`. Der zweite ist der Verweis auf den Seehof und muss als
  solcher lesbar bleiben, nicht als Kleingedrucktes verschwinden. `weide`
  benutzt denselben Namen fuer den Rueckverweis — die beiden Websites zeigen
  aufeinander.
- **D-12** Alle zwoelf Ansichten bleiben erhalten und werden uebersetzt.
- **D-13** Ein `favicon.svg` in der Vorlage samt Rueckfallzeile im `<head>`.
- **D-14** Ein `@media print`-Abschnitt, und zwar in der ERSTEN Aufgabe:
  `TestShippedThemesRenderEveryView` verlangt ihn von jeder mitgelieferten
  Vorlage, und ohne ihn hat der Durchstich kein gruenes Tor. Das war in `weide`
  die erste Abweichung vom Plan.
- **D-15** Kein JavaScript, keine externe Ressource.
- **D-16** Die Vorlage setzt keinen der sechs Reglernamen, sie liest sie nur.
- **D-17** `internal/template/loader.go` braucht keine Aenderung, `rudel` steht
  schon in `BuiltinTemplates`. Der Datenvertrag wird nicht erweitert.
- **D-18** Die Kommentare sind auf DEUTSCH und erklaeren das WARUM.
- **D-19** Kein falsches Risiko noetig: `delnahida.ch` hat keinen A-Eintrag im
  DNS und ist nirgends aufgeschaltet; im Cluster traegt die einzige HTTPRoute
  `holzcloud.ch`. Keine vorsichtigen Zwischenstaende aus Ruecksicht auf
  Besucher.

## Vier Feststellungen aus der Vorbereitung

**Der Kontrast auf diesem Papier, nachgerechnet (D-08).** Tinte `#1B211A` auf
Papier `#F7F6F0` misst 15.17:1. Die schwaechste Tintenstufe, gemischt und auf
dem Papier zusammengesetzt: 52 % (der Wert aus `holzcloud`) ergibt 3.35:1 und
faellt durch; 60 % ergibt 4.25:1 und faellt durch; 62 % ergibt 4.52:1 und liegt
auf der Messerschneide; **64 % ergibt 4.82:1** und ist der Wert, den dieser
Plan festschreibt — dieselbe Prozentzahl wie in `weide`, aber aus einer eigenen
Rechnung, nicht aus einer Uebernahme. Die mittlere Stufe bei 80 % ergibt
8.16:1. Die Marke `#325737` gegen das Papier ergibt 7.59:1 und gegen eine Karte
(Papier mit 18 % Weiss) 7.70:1 — sie traegt damit in beiden Rollen, als
Verweisfarbe und als Knopffuellung mit dem Papierton als Schrift, und das
Verhaeltnis ist symmetrisch.

**Der Kern gibt die Bildmitte nur bei ZUGESCHNITTENEN Bildern mit (D-10).**
`internal/block/render.go` ruft `imgTag` fuer die Galerie (Zeile 165) und fuer
die Karte (Zeile 183) mit gesetztem Zuschnitt-Kennzeichen auf, fuer den
einfachen Bild-Baustein (Zeile 96) ohne. Nur im ersten Fall entsteht das
`style`-Attribut mit dem Bildmittelpunkt (Zeile 367..371). Daraus folgt beides:
die Vorlage darf den Zuschnitt in Galerie und Kartenreihe nicht abschalten —
sonst ist die Angabe tote Auszeichnung —, und sie darf dieselbe Eigenschaft
nicht selbst im Stylesheet setzen, denn eine Regel im Stylesheet kann ein
`style`-Attribut nur mit `!important` schlagen, und genau das waere der
Verlust. Beim einfachen Bild-Baustein gibt es keine Mitte zu steuern, dort
hilft nur die Hoehengrenze.

**`bausteine.css` schneidet heute quer.** `.hc-galerie__bild img` steht auf 4/3,
`.hc-karte img` auf 3/2 (Zeilen 121..145). Beides ist Querformat. Auf einer
Website mit zwoelf Tierportraets im Hochformat schneidet das jedes Mal Kopf
oder Rute ab. Die Vorlage darf das ueberschreiben — `bausteine.css` steht vor
ihr und ist genau eine Klasse tief (Kommentar in Zeile 8).

**Die Vorlage ist heute in der alten Kaskade.** `rudel/style.css` deklariert in
Zeile 43 sechs Schichten (`reset, tokens, base, layout, components, utilities`)
und hat einen `@media print`-Abschnitt INNERHALB der Utilities-Schicht. Beides
verschwindet mit der Neufassung; die Zaehlung der Schichten am Zeilenanfang ist
danach die von `weide`.
</objective>

<execution_context>
@/Users/holz/.claude/gsd-core/workflows/execute-plan.md
@/Users/holz/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

Die Referenz — Aufbau, Umfang und Tonfall dieser Arbeit. Sie ist gebaut,
geprueft und im Browser abgenommen:
@cmd/holzcloud/templates/public/weide/style.css
@cmd/holzcloud/templates/public/weide/layout.html
@cmd/holzcloud/templates/public/weide/home.html
@cmd/holzcloud/templates/public/weide/page.html
@cmd/holzcloud/templates/public/weide/favicon.svg

Was `weide` nach der Sichtpruefung gelernt hat — Pflichtlektuere, die zwei
Befunde stehen hier von Anfang an drin:
@.planning/quick/260903-rqq-vorlage-weide-neu-gestalten-helle-fassun/260903-rqq-SUMMARY.md

Der heutige Stand, der ersetzt wird:
@cmd/holzcloud/templates/public/rudel/layout.html

Die Vertraege:
@internal/tmplspec/TEMPLATE-SPEC.md
@internal/template/loader.go
@internal/design/tokens.go
@cmd/holzcloud/assets/bausteine.css
@internal/block/render.go
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Aufgabe 1: Der Durchstich — Schrift, Gestaltungssystem, Huelle, Textseite</name>

  <files>
cmd/holzcloud/templates/public/rudel/fonts/manrope-latin.woff2
cmd/holzcloud/templates/public/rudel/fonts/manrope-latin-ext.woff2
cmd/holzcloud/templates/public/rudel/favicon.svg
cmd/holzcloud/templates/public/rudel/style.css
cmd/holzcloud/templates/public/rudel/layout.html
cmd/holzcloud/templates/public/rudel/page.html
  </files>

  <read_first>
cmd/holzcloud/templates/public/weide/style.css — Zeilen 1..120 (Dateikopf und
die zwei @font-face), 66..270 (weide.tokens: welche Namen es gibt und in
welcher Ordnung), 666..760 (die CMS-Schicht mit beiden Bruecken) und
1160..1250 (die Navigation als Spalte samt Umschalter — Befund 1 der
Sichtpruefung), 890..975 (der Textkoerper und das Bild allein im Absatz —
Befund 2) sowie 1761..Ende (der Ausdruck).
cmd/holzcloud/templates/public/weide/layout.html — der ganze <head>, der
Sprunglink, die Checkbox vor der Leiste, die Fusszeile.
cmd/holzcloud/templates/public/rudel/layout.html — der heutige Fuss: die
Namen `footer-kontakt` und `schwesterseite`, `.lang-nav`, `.Site.Terms`.
internal/design/tokens.go Zeilen 90..115 — welche sechs Namen ankommen.
  </read_first>

  <reversibility rating="reversible">
`delnahida.ch` hat keinen A-Eintrag und ist nirgends aufgeschaltet (D-19). Die
Vorlage kleidet heute keine oeffentlich erreichbare Seite; ein Rueckbau ist ein
`git revert` und faellt niemandem auf.
  </reversibility>

  <action>
Der Durchstich: eine Textseite laeuft nach dieser Aufgabe end-to-end in der
neuen Gestaltung — Schriftdatei, Gestaltungssystem, Huelle, Ansicht. Die zehn
uebrigen Ansichten bleiben vorerst stehen und rendern weiter (sie sind gueltige
Go-Templates), damit der Pruefbefehl in jeder Aufgabe laufen kann.

**Die Schriften (D-04).** Kopiere `manrope-latin.woff2` und
`manrope-latin-ext.woff2` aus `cmd/holzcloud/templates/public/weide/fonts/`
nach `cmd/holzcloud/templates/public/rudel/fonts/`. Kopieren, nicht
herunterladen: die Dateien sind in VENDOR.md mit Groesse, Quelle, SHA-256 und
Lizenz verzeichnet und liegen bereits zweimal im Repository. Keine Schrift
fester Breite.

**`style.css`, Teil 1 — der Dateikopf (D-01, D-18).** Ein deutscher Kommentar,
der die drei Stufen der Kaskade erklaert und warum unten keine Schicht stehen
darf. Kommentarzeilen im Rumpf bleiben eingerueckt, damit die Zaehlung der
Schichten am Zeilenanfang eindeutig bleibt.

**`style.css`, Teil 2 — die Schriften.** Zwei `@font-face`-Bloecke fuer Manrope,
je einer fuer latin und latin-ext, mit einer Gewichtsspanne von 200 bis 800,
`font-display: swap`, `src: url("/t/fonts/manrope-latin.woff2")
format("woff2")` und den `unicode-range`-Angaben aus weide/style.css
unveraendert uebernommen. Ein deutscher Kommentar sagt, warum eine Spanne und
nicht ein Block je Schnitt, und warum die zwei Teilmengen getrennt sind.

**`style.css`, Teil 3 — vier Gestaltungsschichten**, in dieser Reihenfolge:
`@layer rudel.tokens`, `@layer rudel.base`, `@layer rudel.components`,
`@layer rudel.motif`. Genau vier, alle vor der CMS-Schicht.

*rudel.tokens* — die einzige Stelle, an der ein Farbwert entschieden wird
(D-07). Vier Entscheidungen: `--hc-ground` auf `#F7F6F0`, `--hc-ink` auf
`#1B211A`, `--hc-wald` auf `#325737`, `--hc-on-wald` auf `var(--hc-ground)`.
Der Name `--hc-wald` ersetzt das `--hc-erde` von weide — Waldgruen statt
Erdbraun, und der Name macht jede Regel darunter lesbar. Alle weiteren Farben
sind Ableitungen ueber `color-mix(in srgb, …)`:
`--hc-ink-2` auf 80 % Tinte (gemessen 8.16:1);
`--hc-ink-3` auf 64 % Tinte (gemessen 4.82:1) — diese Zeile traegt einen
deutschen Kommentar mit der eigenen Rechnung: 52 % ergaeben hier 3.35:1 und
60 % ergaeben 4.25:1, beide fallen durch; 62 % ergibt 4.52:1 und liegt auf der
Messerschneide; 64 % ist der Wert mit Luft (D-08);
`--hc-hairline` auf 14 % Tinte, `--hc-pane-edge` auf 12 % Tinte;
`--hc-pane` auf Papier mit 18 % Weiss (eine Karte liegt heller als das Blatt),
`--hc-pane-sunken` auf Papier mit 6 % Tinte;
`--hc-pane-bar` auf Papier mit 88 % Deckung, `--hc-pane-bar-solid` auf 98 %;
`--hc-wald-soft` auf 35 % Marke, `--hc-wald-wash` auf 8 % Marke;
`--hc-focus` auf `var(--hc-wald)`.
Der Grund (D-07): `--hc-ground-2` auf Papier mit 4 % Weiss; drei sehr leise
radiale Schleier — `--hc-wash-gruen` auf 9 % der Marke, `--hc-wash-moos` auf
8 % eines Mooses (Richtwert `#6B7F4A`), `--hc-wash-sand` auf 7 % eines warmen
Sands (Richtwert `#C9A87C`). Moos und Sand sind hier als Literal richtig, weil
sie eine zweite und dritte Farbe sind und nicht die Marke; das Gruen leitet aus
der Marke ab, damit eine eingestellte Marke den Grund mitzieht.
`--hc-ground-image` fasst die drei Schleier ueber einem `linear-gradient` von
`--hc-ground-2` nach `--hc-ground` zusammen; beide Enden leiten aus dem
Papierwert ab, sonst wirkt ein anderes Papier nur an einem Ende.

*Die Radienfamilie ist der Ort, an dem sich `rudel` von `weide` unterscheidet
(D-09).* In `weide` stehen die kleine und die grosse Rundung als feste Werte da,
und eine eingestellte Rundung bewegt nur die mittlere. Hier haengt die ganze
Familie an der mittleren: `--hc-r-sm` auf `calc(var(--hc-r) * 0.55)`,
`--hc-r-lg` auf `calc(var(--hc-r) * 1.6)`, `--hc-r-pill` auf `999px`. Der
Entwurfswert der mittleren Rundung steht in der Bruecke weiter unten und ist
16px — eine Stufe runder als die 14px von `weide`, weil diese Website mit 10
eine Rundung waehlt und jene mit 0 keine. Ein deutscher Kommentar haelt fest,
warum die Ableitung ueber `calc()` und nicht ueber drei feste Werte laeuft: das
Manifest der Website stellt genau eine Zahl ein, und sie soll die Ecken der
Karte, des Bildes und des Eingabefeldes im selben Verhaeltnis mitziehen.

Weiter die Namensfamilien, die die CMS-Schicht braucht, nach dem Vorbild von
weide.tokens: `--hc-space-1` bis `--hc-space-9` in 4px-Schritten; die
Schriftgrade `--hc-text-label`, `--hc-text-xs`, `--hc-text-sm`,
`--hc-text-base`, `--hc-text-lg`, `--hc-text-xl`, `--hc-text-2xl`,
`--hc-display-sm`, `--hc-display` — die vier groessten fluid ueber `clamp()`,
die Schlagzeile mit dem Richtwert `clamp(2.4rem, 5.5vw, 4.2rem)`; die Gewichte
`--hc-weight-thin` 250, `--hc-weight-regular` 400, `--hc-weight-medium` 500,
`--hc-weight-semibold` 650; `--hc-track-snug` leicht negativ, `--hc-track-wide`
und `--hc-track-widest` gesperrt fuer Versalien-Beschriftungen (es gibt keine
Familie fester Breite, also auch keinen Namen dafuer); `--hc-leading-tight` um
1.03; `--hc-shadow-pane` und `--hc-shadow-lift` als weiche, aus der Tinte
gemischte Schatten; `--hc-dur-fast`, `--hc-ease`; `--hc-wide` auf 1320px,
`--hc-gutter` auf `clamp(20px, 4vw, 56px)`, `--hc-measure` auf `74ch`;
`--hc-font-sans` auf `Manrope, ui-sans-serif, system-ui, -apple-system,
sans-serif`.

*rudel.base* — Grundreset, `body` mit `background: var(--hc-ground-image)`,
`background-attachment: fixed` und der Tinte als Schriftfarbe, die Schrift aus
`--hc-font-sans`, `-webkit-font-smoothing: antialiased`, ein sichtbarer
Fokusring aus `--hc-focus`, `:target`-Sprungabstand, `prefers-reduced-motion`.
Die Ueberschriftengroessen und die Textgrundlinie.

*rudel.components* — dieselben Bauteilnamen wie in `weide`, damit dieselben
Gesten dieselben Namen tragen: `.hc-wide`, `.hc-bar` und `.hc-bar__inner`,
`.hc-pane`, `.hc-btn` mit den Spielarten, `.hc-input`, `.hc-select`,
`.hc-textarea`, `.hc-field`, `.hc-label`, `.hc-hint`, `.hc-error`, `.hc-alert`,
`.hc-status`, `.hc-chip`, `.hc-eyebrow`, `.hc-display`, `.hc-title`,
`.hc-claim`, `.hc-lede`, `.hc-dim`, `.hc-defs`, `.hc-table`, `.hc-stack`,
`.hc-row`, `.hc-sr-only`, `.hc-lbl`. `.hc-pane` ist ein deckender Kasten mit
Haarlinie und weichem Schatten, keine Glasflaeche. `.hc-eyebrow` und `.hc-lbl`
sind gesperrte Versalien. `.hc-bar` klebt oben, liegt auf `--hc-pane-bar`,
traegt eine Haarlinie unten und als einzige Flaeche der Vorlage einen
`backdrop-filter` — mit einem `@supports`-Zweig und einem Zweig fuer
`prefers-reduced-transparency`, die beide auf die deckende Fassung schalten. In
`weide` kostete genau diese Stelle einen Nachbesserungs-Commit: bei blosser
Deckung geisterte der Fliesstext von unten als graue Schrift durch.
`.site-mark` steht auf `flex: 0 1 auto` mit `min-width: 0` und der Name auf
`overflow-wrap: break-word` — „Hundezucht Delnahida" ist lang, und ein Element,
das auf seiner `max-content`-Breite besteht, schiebt die Leiste auf einem
Telefon nach rechts hinaus.

*rudel.motif* — das Zeichenvokabular fuer die schmueckende Zeichnung: ein
`.hc-waldsaum` fuer das svg und dazu `.hc-w-fern`, `.hc-w-nah`, `.hc-w-boden`,
`.hc-w-licht`, alle Farben aus `--hc-wald`, `--hc-ink` und `--hc-ground`
gemischt. Ein ruhiger Waldsaum mit einem Weg davor, weil die Vorlage `rudel`
heisst und ein Rudel draussen laeuft. Die Zeichnung selbst kommt in Aufgabe 2;
die Klassen entstehen hier, weil sie zum Gestaltungssystem gehoeren und nicht
zur Ansicht.

**`style.css`, Teil 4 — die CMS-Schicht (D-01).** Eingeleitet von einer
Markierungszeile, die am Zeilenanfang mit `/* ══ CMS` beginnt, gefolgt von
einem deutschen Kommentar: hier steht kein Farbwert, und hier steht bewusst
keine Schicht, denn eine fuenfte wuerde diesen Bereich wieder unter die eigenen
vier und unter `.Site.Design` stellen.

*Die Bruecke zu den Reglern (D-02, D-16).* Ein `:root`-Block mit genau vier
Zeilen, jede liest den Regler mit dem Entwurfswert als Rueckfall: `--hc-wald`
aus `var(--hc-brand, #325737)`, `--hc-ground` aus `var(--hc-paper, #F7F6F0)`,
`--hc-font-sans` aus `var(--hc-font, Manrope, ui-sans-serif, system-ui,
-apple-system, sans-serif)`, `--hc-r` aus `var(--hc-radius, 16px)`. Die zwei
uebrigen Reglernamen wirken allein durch ihren Namen, weil das System sie schon
so nennt — sie brauchen keine Zeile. Ein deutscher Kommentar haelt fest, dass
die Richtung immer vom Regler zum System geht: die Rueckrichtung waere ein
Zyklus im benutzerdefinierten Wert und beide Werte fielen aus.
**Wichtig fuer das Prueftor:** die vier gebrueckten Reglernamen duerfen in der
ganzen Datei nur INNERHALB einer `var()`-Klammer stehen, nie links von einem
Doppelpunkt — auch nicht in einem Kommentar. Das Tor liest die Datei als Text
und unterscheidet Kommentar und Regel nicht.

*Die Bruecke zu den Bausteinen (D-03).* Ein zweiter `:root`-Block mit zehn
Zeilen auf die generischen Namen, die `bausteine.css` in den Zeilen 19..25
liest: `--ink`, `--paper`, `--brand`, `--line`, `--space-3`, `--space-4`,
`--step--1`, `--step-1`, `--step-2`, `--radius`. Ein deutscher Kommentar sagt,
warum `--paper` der Seitengrund ist und nicht die Kartenflaeche: `.hc-knopf`
setzt ihn als Schriftfarbe auf die Markenflaeche. Diese zehn Zeilen kleiden
alle neun Bausteinarten auf einmal; wer stattdessen `.hc-block`-Regeln
nachbaut, hat zwei Gestaltungen zu pflegen und behaelt die zweite nicht.

*Sprunglink und Huelle.* `.skip-link` heisst woertlich so — es ist die
Verabredung aller mitgelieferten Vorlagen und die Testreihe sucht den String.
Geklemmt statt nach -9999px geschoben. Dazu `.sr-only`, `html`
`scroll-padding-top`, `main`, `.page-wrap`.

*Die Kopfleiste und die Navigation (D-05).* `.site-mark`, `.site-logo`,
`.site-menu` mit Untermenues ueber `:hover` und `:focus-within` (Sichtbarkeit
und Deckkraft, kein `display`, damit der Uebergang etwas zu animieren hat),
`.site-cart`, `.site-search`, `.lang-nav` (der Name bleibt, `bausteine.css`
kleidet ihn schon grund). Oberhalb der Schwelle stehen Checkbox UND
Beschriftung auf `display: none` — damit sind beide aus dem Tabulatorweg und
aus dem Baum der Hilfsmittel heraus.
Unterhalb von 1000 px, und das ist die Regel, die `weide` einen
Nachbesserungs-Commit gekostet hat: die oberste Menueliste und das Untermenue
stehen BEIDE ausdruecklich in Spaltenrichtung und auf `align-items: stretch`,
jedes `<li>` auf volle Breite. Bleibt die oberste Liste eine Zeile mit
Umbruch, wird ein `<li>` mit Untermenue darin zum breiten Flex-Element, dessen
Verweis und dessen jetzt statische Liste nebeneinander laufen — die Zeile
bricht um dieses breite Element herum, und daraus entsteht ein Treppenmuster
ueber drei Einzuege. Wer das eines Tages „aufraeumt", holt es zurueck.
Dazu der Umschalter: die Checkbox bleibt unterhalb der Schwelle sichtbar
geklemmt und fokussierbar (hier NICHT `display: none`, das naehme ihr den
Fokus), der Fokusring erscheint ueber den Geschwisterselektor an der
Beschriftung; zugeklappt heisst fuer das Menue `display: none`, damit auch die
Punkte der Untermenues nicht im Tabulatorweg liegen. Sechs Hauptpunkte, zwei
davon mit Untermenue, sind ohne Umschalter rund 320 px Leiste, bevor auf einem
Telefon der erste Inhalt kommt — auf jeder Seite. Hat eine Website gar kein
Hauptmenue, gibt `menuFor` eine leere Zeichenkette zurueck
(internal/menu/render.go:20); dann faellt der Knopf ueber
`.hc-bar__inner:not(:has(.site-menu ul))` weg, statt ein leeres Fach zu
oeffnen. Unter 600 px verschwindet die Suche aus der Leiste, klappt aber mit
dem Menue an derselben Checkbox auf — ganz wegzunehmen hiesse, dass ein Telefon
die Suche nur noch ueber die 404-Seite findet.

*Die Fusszeile (D-11).* Mehrspaltiges Raster fuer Marke, Fussnavigation,
`footer-kontakt`, `schwesterseite` und Schlagwoerter, dazu
`.site-footer__mark`, `.site-footer__heading`, `.site-footer__legal`. Der
Baustein `schwesterseite` behaelt seinen eigenen Klassennamen und steht in der
Grundschriftgroesse in `--hc-ink-2`, nicht in der kleinsten Stufe: er ist der
Verweis auf die Schwesterwebsite und muss sich als Satz lesen. Ein deutscher
Kommentar nennt den Grund und den Umstand, dass `weide` denselben Namen fuer
den Rueckverweis benutzt — die beiden Websites zeigen aufeinander.

*Der Textkoerper und das Bild allein im Absatz (D-06).* `.page-content` als
Zweispalten-Raster nach dem Vorbild von weide/style.css Zeile 899: eine
Textspalte auf dem Mass aus `--hc-measure`, daneben eine zweite bis
`breit-ende`; `.hc-galerie`, `.hc-video` und `.hc-bild--voll` laufen bis dorthin.
Ein deutscher Kommentar sagt, warum zwei Rasterspalten und nicht `max-width` am
Elternteil: die zweite Spalte ist unter dem Mass null breit, also stapelt sich
das auf dem Telefon von selbst.
Dazu — und das ist Befund 2 der Sichtpruefung, hier im ersten Wurf — die Regel
fuer ein Bild, das allein in seinem Absatz steht: der Selektor
`.page-content > p:has(> :is(img, picture):only-child)` zusammen mit
`.page-content > figure:not(.hc-block):has(> img)` bekommt `grid-column` bis
`breit-ende`, Luft davor und danach; das Bild darin steht auf `inline-size:
auto` mit `max-inline-size: 100%` und `max-block-size: 70vh`, mittig, mit
Haarlinie und `--hc-r`. Kein `cover` und kein `contain`: ein Zuschnitt schneidet
Hunde an, und `contain` legte leere Streifen INNERHALB der Haarlinie ab. Der
Kommentar nennt die Begruendung samt der Zahl aus `weide` — auf einer Seite aus
reinem Markdown standen bei 1265 px Fensterbreite ueber 700 px leer neben einem
440 px schmalen Streifen — UND den Grund, warum die Hoehengrenze hier die
tragende Regel ist und nicht die Vorsichtsmassnahme: diese Website lebt von 85
Hundefotos, viele davon Hochformat, und ein Hochformat ueber die volle Breite
waere anderthalb Bildschirme hoch. Nur bei `:only-child`; ein Browser ohne
`:has()` laesst beide Regeln fallen und zeigt das Bild in der Textspalte, also
den Zustand von vorher.
Aus demselben Grund und mit demselben Kommentar bekommen `.hc-bild--voll img`
und `.hc-bild--breit img` dieselbe Hoehengrenze und dieselbe natuerliche
Breite: der Kern gibt fuer den einfachen Bild-Baustein keine Bildmitte mit
(internal/block/render.go:96), dort gibt es also nichts zu steuern, und ein
Zuschnitt waere geraten.
Dazu die Innengestaltung des Fliesstextes (Ueberschriften mit dem grossen
Abstand VOR sich, Listen, Verweise in der Marke, Tabellen, `pre`, `mark`) und
die Moeblierung von `page.html`: `.preview-banner`, `.page-header`,
`.page-date`, `.page-fields`, `.term-list`, `.term`, `.post-nav`.

*Der Ausdruck (D-14).* Als letzte Regel der Datei ein `@media print`-Abschnitt.
`TestShippedThemesRenderEveryView` verlangt ihn von jeder mitgelieferten
Vorlage; ohne ihn ist die Testreihe schon nach dieser Aufgabe rot. Der Grund
ist hier ohnehin hell, also bleibt der Abschnitt kurz: der Farbschleier wird
abgeschaltet, Leiste, Menue, Sprachwahl, Suche, Korb, Sprunglink,
Nachbarnavigation und Blaetterleiste verschwinden, Schatten fallen weg,
Adressen von Verweisen werden hinter dem Linktext ausgeschrieben. Der Baustein
`footer-kontakt` bleibt stehen — wer die Seite eines Zwingers ausdruckt, will
meistens genau diese Zeilen. Die drei Literalfarben, die ein Druckstylesheet
braucht, sind hier bewusst Literale: auf Papier ist Weiss das Papier des
Druckers und kein Wert der Palette. Aufgabe 2 und 3 fuegen ihre Regeln DAVOR
ein, damit dieser Abschnitt die letzte Regel bleibt.

**`favicon.svg` (D-13).** Ein Zeichen aus dem Vokabular der Vorlage — eine
ruhige Pfote oder der Waldsaum —, mit festen Farbwerten, weil ein Favicon
nichts erbt: Waldgruen auf dem Papierton. Keine externe Referenz, kein
`<script>`.

**`layout.html` (D-05, D-11, D-13, D-15).** Nach dem Muster von
weide/layout.html:
Im `<head>` alle Meta-Angaben der heutigen Fassung unveraendert (Titel,
description, robots, canonical, og:*, twitter:card, der
`application/ld+json`-Datenblock, das Atom-Feed, die hreflang-Zeilen), dazu neu
die Zeile `<meta name="color-scheme" content="light">` und die Rueckfallzeile
fuer das Favicon, wenn die Website keines gesetzt hat — ohne sie fragt der
Browser bei jedem Aufruf `/favicon.ico` an und bekommt einen 404. Die
Stylesheets in der Reihenfolge `bausteine.css`, `/t/style.css`, dann der
`<style>` mit `.Site.Design`. Kein preconnect, kein Stylesheet zu einem
Schrifthaus.
Der `<body>` beginnt mit dem Sprunglink als erstem Kind und damit erstem
Tabulatorziel; DANACH die Checkbox als Geschwister vor der Leiste, damit der
Geschwisterselektor greift.
Die Leiste traegt Marke (Logo, sonst Name), Hauptnavigation ueber `menuFor`,
die Sprachwahl, und neu — die heutige Fassung hat beides nicht, obwohl sie
`shop.html` und `cart.html` mitbringt — den Warenkorb hinter `.Shop.Enabled`
und das Suchformular hinter `.Site.HasSearch`. Beides muss `MinimalData()`
ueberstehen, wo `Shop` und `Search` leer sind.
Die Fusszeile behaelt alles, was die heutige Fassung zeigt: Name, Beschreibung,
Fussnavigation, die beiden Textbausteine `footer-kontakt` und
`schwesterseite` mitsamt dem eigenen Klassennamen des zweiten, die
Schlagwoerter aus `.Site.Terms` und die Zeile mit dem Copyright. Statt des
Schafs aus `weide` ein eingebauter Pfad in `currentColor`, der zu dieser
Vorlage passt.

**`page.html`.** Nach dem Muster von weide/page.html: Vorschau-Banner,
schlanker Kopf (Titel nur wenn `not .Page.HasOwnHeading`, Datum nur am
Beitrag), `.page-content` mit `.Page.ContentHTML`, die Feldliste mit allen acht
Faellen unveraendert aus der heutigen Fassung uebernommen, Schlagwoerter, und
die Nachbarnavigation mit `{{with}}` statt `{{if}}` — `Prev` und `Next` sind am
Rand des Archivs nil. Die heutige Fassung zeigt beim Nachbarn auch dessen
Titel; das bleibt.

Jede Datei traegt deutsche Kommentare, die das WARUM erklaeren und nicht das
WAS (D-18). Kein `.js`, kein `<script>` ausser dem Datenblock, kein `on*`, kein
`javascript:`, keine Adresse zu einem fremden Ursprung (D-15).
  </action>

  <verify>
    <automated>
cd /Users/holz/Projects/holzcloud-cms
cmp cmd/holzcloud/templates/public/weide/fonts/manrope-latin.woff2 cmd/holzcloud/templates/public/rudel/fonts/manrope-latin.woff2
cmp cmd/holzcloud/templates/public/weide/fonts/manrope-latin-ext.woff2 cmd/holzcloud/templates/public/rudel/fonts/manrope-latin-ext.woff2
test -f cmd/holzcloud/templates/public/rudel/favicon.svg
awk '/^@layer/{n++; last=NR} /^\/\* ══ CMS/{cms=NR} END{ if (n==4 && cms>0 && last<cms) print "Kaskade ok: 4 Schichten, alle vor der CMS-Schicht"; else { print "FEHLER: Schichten=" n " letzte=" last " CMS=" cms; exit 1 } }' cmd/holzcloud/templates/public/rudel/style.css
test "$(grep -c -E -- '--hc-(brand|paper|font|radius)[[:space:]]*:' cmd/holzcloud/templates/public/rudel/style.css)" = 0 && echo "Regler werden nur gelesen, nicht gesetzt"
grep -q -- '--hc-ink-3:.*var(--hc-ink) 64%' cmd/holzcloud/templates/public/rudel/style.css && echo "Kontraststufe 64% (4.82:1) gesetzt"
grep -q '@media print' cmd/holzcloud/templates/public/rudel/style.css && echo "Druck-Stylesheet vorhanden"
perl -0777 -pe 's{/\*.*?\*/}{}gs' cmd/holzcloud/templates/public/rudel/style.css | perl -0777 -ne 'exit(/\.site-menu ul\s*\{[^}]*flex-direction:\s*column/s ? 0 : 1)' && echo "Menue ist unter der Schwelle eine Spalte"
perl -0777 -ne 'exit(/<body[^>]*>\s*(?:<!--.*?-->\s*)*<a href="#main" class="skip-link"/s ? 0 : 1)' cmd/holzcloud/templates/public/rudel/layout.html && echo "Sprunglink ist erstes Kind von body"
grep -q 'class="nav-switch"' cmd/holzcloud/templates/public/rudel/layout.html && echo "Umschalter als Checkbox vorhanden"
grep -q 'footer-kontakt' cmd/holzcloud/templates/public/rudel/layout.html && grep -q 'schwesterseite' cmd/holzcloud/templates/public/rudel/layout.html && echo "beide Fuss-Textbausteine erhalten"
test "$(perl -0777 -pe 's{/\*.*?\*/}{}gs' cmd/holzcloud/templates/public/rudel/style.css | grep -c 'max-block-size')" -ge 2 && echo "Hoehengrenze steht am Markdown-Bild und am Bild-Baustein"
go build ./...
go run ./cmd/holzcloud template check cmd/holzcloud/templates/public/rudel
go test ./internal/template/... ./internal/tmplmgr/...
    </automated>
  </verify>

  <done>
`template check` meldet „no problems found". Die beiden woff2 sind byteweise
identisch mit den Kopien in `weide`. `style.css` traegt genau vier Schichten,
alle vor der Markierung der CMS-Schicht, setzt keinen der vier gebrueckten
Reglernamen und hat einen `@media print`-Abschnitt als letzte Regel. Die
schwaechste Tinte steht auf 64 % mit der eigenen Rechnung im Kommentar. Das
Menue ist unter der Schwelle eine Spalte, der Umschalter steht als Checkbox
hinter dem Sprunglink, und beide Fuss-Textbausteine sind erhalten. `go build`
und die beiden Testpakete sind gruen. Eine Textseite laeuft end-to-end in der
neuen Gestaltung; die zehn uebrigen Ansichten stehen noch in der alten und
rendern weiter.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Aufgabe 2: Der Aufmacher und die neun Bausteinarten im Hochformat</name>

  <files>
cmd/holzcloud/templates/public/rudel/home.html
cmd/holzcloud/templates/public/rudel/style.css
  </files>

  <read_first>
cmd/holzcloud/templates/public/weide/home.html — der Aufbau des Aufmachers und
der Kommentar dazu, warum `.Page.Excerpt` dort NICHT steht.
cmd/holzcloud/templates/public/weide/style.css — Zeilen 1247..1410: der
Aufmacher und die Bausteine in der CMS-Schicht.
internal/block/render.go — die Auszeichnung der neun Bausteinarten, besonders
die Zeilen 86..96 (Bild-Baustein, ohne Zuschnitt), 156..175 (Galerie, mit
Zuschnitt), 176..208 (Karten, mit Zuschnitt) und 353..372 (`imgTag`: wo das
`style`-Attribut mit dem Bildmittelpunkt entsteht und unter welcher Bedingung).
Sie ist die einzige Wahrheit ueber die Klassennamen, nicht dieser Plan.
cmd/holzcloud/assets/bausteine.css Zeilen 95..160 — was die Bruecke schon
leistet und welche zwei Seitenverhaeltnisse sie heute vorgibt.
  </read_first>

  <action>
**`home.html`.** Ein grosser Aufmacher, zweispaltig, nach dem Aufbau von
weide/home.html:
Links die Augenbrauen-Zeile aus `.Site.Name` als gesperrte Versalien in der
Marke, darunter die Schlagzeile aus `.Page.Title` — nur wenn
`not .Page.HasOwnHeading`, sonst begruesst die Startseite zweimal —, darunter
eine Zeile Anspruch aus `.Site.Description`, darunter die Knoepfe. Die Knoepfe
zeigen nur auf Orte, die es wirklich gibt: der Laden hinter `.Shop.Enabled` auf
`.Shop.URL`, das Archiv hinter `{{with .Page.ArchiveURL}}`, die Suche hinter
`.Site.HasSearch`. Jedes Stueck einzeln abgesichert — eine Seite, die nichts
als einen Titel hat, muss trotzdem rendern.
`.Page.Excerpt` steht NICHT im Aufmacher. Der Anriss wird beim Speichern aus
demselben Markdown abgeleitet, das zwei Abschnitte weiter unten als Inhalt noch
einmal kommt; in `holzcloud` stand die Begruessung dadurch zweimal
untereinander, und das war der einzige Fehler, den die Sichtpruefung dort fand.
Ein deutscher Kommentar haelt das fest, damit es nicht zurueckkommt.

Rechts das Titelbild der Seite aus `{{with .Meta.OGImage}}`, im `{{else}}`-Zweig
die schmueckende Waldsaum-Zeichnung. Das Bild traegt `loading="eager"`,
`decoding="async"` und ein leeres `alt`, weil es schmueckend neben der
Schlagzeile steht und nicht der Inhalt ist. Ein deutscher Kommentar sagt, woher
der Wert kommt (`internal/public/pagedata.go`, `withOGImage`, aus dem Titelbild
der Seite) und dass er die absolute Adresse auf den eigenen Ursprung ist —
dieselbe, die zwei Zeilen weiter oben schon als `og:image` im Kopf steht, also
holt der Browser sie einmal.
Die Zeichnung ist ein `<svg>` mit `aria-hidden="true"`,
`role="presentation"` und `focusable="false"`, gebaut allein aus den
Motiv-Klassen aus `rudel.motif` — kein `fill` und kein `stroke` als Literalwert
im Markup, sonst folgt sie einer eingestellten Marke nicht.
Darunter der Rumpf: `.page-content` mit `.Page.ContentHTML`, sonst ein
Willkommenssatz, dann die Schlagwoerter.

**`style.css` — der Aufmacher (D-06, D-10).** Vor den Druck-Abschnitt anfuegen:
`.site-hero`, `.site-hero-grid` (zwei Spalten, auf schmalen Fenstern
untereinander), `.site-actions` mit `:empty { display: none }`, damit auf einer
nackten Website kein Abstand uebrigbleibt, `.site-art` und `.site-art__bild`.
Und hier weicht `rudel` bewusst von `weide` ab: das Aufmacherbild wird NICHT
zugeschnitten. `weide` zwingt es in ein Verhaeltnis von 4/3 mit `cover`; das
Titelbild ist aber ein blosser URL ohne jede Angabe zur Bildmitte, und ein
Zuschnitt auf Querformat trifft bei einem hochformatigen Hundeportraet mit
hoher Wahrscheinlichkeit den Kopf. Stattdessen `inline-size: auto` mit
`max-inline-size: 100%`, `block-size: auto` und `max-block-size: min(70vh,
620px)`, mittig in seiner Spalte, mit `--hc-r-lg`, Haarlinie und
`--hc-shadow-pane`. Ein Querformat fuellt damit die Spalte, ein Hochformat
steht ganz und ruhig daneben. Der deutsche Kommentar nennt beide Haelften des
Handels: ohne Zuschnitt ist die Spaltenhoehe nicht mehr konstant, dafuer geht
kein Gesicht verloren.

**`style.css` — die neun Bausteinarten (D-03, D-10).** Nur was aus der Bruecke
NICHT folgt, mit je einem deutschen Kommentar, warum die Zeile noetig ist. Der
Abschnitt beginnt mit einer Markierungszeile, die am Zeilenanfang mit
`/* ══ Die Bausteine` steht, und endet vor der naechsten solchen Markierung —
das Prueftor liest diesen Bereich fuer sich.
- Bild, Galeriebild und eigenes Bild bekommen eine Haarlinie, damit ein helles
  Bild auf hellem Papier als Fenster liest und nicht ausfranst.
- Bildunterschriften werden gesperrte Versalien in `--hc-ink-3`; die `opacity`
  aus `bausteine.css` wird dabei auf 1 zurueckgesetzt, weil die Farbe die
  Abstufung schon traegt und beides uebereinander unter die Kontrastgrenze
  fiele.
- **Das Kachelraster traegt Hochformat (D-10).** `bausteine.css` schneidet das
  Galeriebild auf 4/3 und das Kartenbild auf 3/2, beides Querformat. Diese
  Website hat zwoelf Tierportraets und 85 Bilder, viele davon hochkant; ein
  Querformat-Zuschnitt nimmt davon jedes Mal Kopf oder Rute. Die Vorlage setzt
  das Galeriebild deshalb auf `1 / 1` und das Kartenbild auf `4 / 3`. Ein
  Quadrat ist die ehrliche Mitte: es kostet ein Hochformat und ein Querformat
  denselben Anteil, waehrend ein hochkantes Verhaeltnis die Querformate haerter
  bestrafen wuerde, als das Quadrat beide bestraft. Der Zuschnitt selbst BLEIBT
  — `internal/block/render.go` gibt fuer genau diese zwei Bausteinarten je Bild
  einen Mittelpunkt als `style`-Attribut mit (Zeile 367..371), und der wirkt
  nur, solange das Bild in eine feste Form gezwungen wird. Die Vorlage setzt
  diese Eigenschaft an keiner Stelle des Baustein-Abschnitts selbst: eine Regel
  im Stylesheet koennte ein `style`-Attribut nur mit `!important` schlagen, und
  genau das waere der Verlust der Angabe. Ein deutscher Kommentar haelt beide
  Haelften fest.
- `.hc-karte` ist ein deckender Kasten: `--hc-pane` als Grund, `--hc-pane-edge`
  als Kante, `--hc-shadow-pane` als Schatten, KEIN `backdrop-filter`.
  `.hc-karte--link` hebt beim Ueberfahren an und wechselt auf
  `--hc-shadow-lift`, gebunden an `prefers-reduced-motion`.
- `.hc-zitat`: der Balken traegt die Marke, nicht die Schriftfarbe — ein ganzer
  Absatz in Waldgruen laese sich als Werbung; die Beschriftung in gesperrten
  Versalien.
- `.hc-aufruf`: dieselbe deckende Flaeche wie eine Karte, eine Stufe groesser,
  mit `--hc-r-lg`.
- `.hc-knopf`: die Pillenform, und ausdruecklich der Papierton als Schriftfarbe.
  Der Kommentar nennt den Grund, den `holzcloud` teuer gelernt hat: die Regel
  fuer Verweise im Inhalt faerbt weiter oben jeden Link in der Marke, steht
  spaeter in derselben Datei, und ohne diese Zeile stuende der Knopf als
  gruener Text auf gruener Flaeche.
- `.hc-trenner`: Abstand statt Auffaelligkeit.
- `.hc-video`: hier fragt der Kern die Reglernamen der Verwaltung direkt ab und
  fiele ohne sie auf 0 zurueck. Die Regel zielt deshalb auf die Klasse und
  nicht auf `:root` — am `:root` waere es der Zyklus, den die Bruecke
  vermeidet.
- Absaetze innerhalb eines Bausteins (`.hc-text`, `.hc-eigen__text`,
  `.hc-bildtext__text`) brauchen ihren eigenen Rhythmus: die Grundschicht setzt
  Absaetze auf `margin: 0`, und die Regel fuer den Abstand zwischen Bausteinen
  trifft nur die Bausteine selbst — ein Textbaustein aus drei Absaetzen stuende
  sonst als eine Wand ohne Luft.
- `.hc-bildtext__bild img` bekommt dieselbe Haarlinie, aus demselben Grund.
- `.hc-eigen`: die Felder als ruhige Faktenzeilen in `--hc-ink-2`. Eigene
  Bausteinarten gehoeren der Website, nicht der Vorlage — hier steht nur, was
  ohne Kenntnis des Schluessels richtig ist.
  </action>

  <verify>
    <automated>
cd /Users/holz/Projects/holzcloud-cms
grep -q 'Meta.OGImage' cmd/holzcloud/templates/public/rudel/home.html && echo "Titelbild im Aufmacher verdrahtet"
grep -q 'loading="eager"' cmd/holzcloud/templates/public/rudel/home.html && echo "Aufmacherbild laedt sofort"
grep -q -- '\.hc-galerie__bild img' cmd/holzcloud/templates/public/rudel/style.css && grep -q -- '\.hc-karte img' cmd/holzcloud/templates/public/rudel/style.css && echo "beide Kachelbilder haben eine eigene Regel"
test "$(awk '/^\/\* ══ Die Bausteine/{f=1;next} /^\/\* ══ /{f=0} f' cmd/holzcloud/templates/public/rudel/style.css | perl -0777 -pe 's{/\*.*?\*/}{}gs' | grep -c 'object-position')" = 0 && echo "die Bildmitte aus dem Kern bleibt unangetastet"
test "$(awk '/^\/\* ══ Die Bausteine/{f=1;next} /^\/\* ══ /{f=0} f' cmd/holzcloud/templates/public/rudel/style.css | grep -c 'aspect-ratio')" -ge 2 && echo "Galerie und Karte tragen ein eigenes Seitenverhaeltnis"
awk '/^@layer/{n++; last=NR} /^\/\* ══ CMS/{cms=NR} END{ if (n==4 && cms>0 && last<cms) print "Kaskade ok"; else { print "FEHLER"; exit 1 } }' cmd/holzcloud/templates/public/rudel/style.css
test "$(grep -c -E -- '--hc-(brand|paper|font|radius)[[:space:]]*:' cmd/holzcloud/templates/public/rudel/style.css)" = 0 && echo "Regler werden nur gelesen"
go build ./...
go run ./cmd/holzcloud template check cmd/holzcloud/templates/public/rudel
go test ./internal/template/... ./internal/tmplmgr/...
    </automated>
  </verify>

  <done>
`template check` meldet „no problems found" — und weil es jede Ansicht gegen
SampleData UND gegen MinimalData rendert, ist damit sowohl der Zweig mit
Titelbild als auch der mit der Waldsaum-Zeichnung bewiesen. Galerie und
Kartenreihe tragen ein Seitenverhaeltnis, das Hochformat vertraegt, und der
Baustein-Abschnitt setzt die Bildmitte nirgends selbst — die Angabe aus dem
Kern wirkt weiter. Das Aufmacherbild wird nicht zugeschnitten und ist in der
Hoehe begrenzt. Die Kaskade traegt weiterhin genau vier Schichten.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Aufgabe 3: Die zehn uebrigen Ansichten, der Laden und die Herkunft</name>

  <files>
cmd/holzcloud/templates/public/rudel/list.html
cmd/holzcloud/templates/public/rudel/search.html
cmd/holzcloud/templates/public/rudel/gate.html
cmd/holzcloud/templates/public/rudel/404.html
cmd/holzcloud/templates/public/rudel/maintenance.html
cmd/holzcloud/templates/public/rudel/shop.html
cmd/holzcloud/templates/public/rudel/product.html
cmd/holzcloud/templates/public/rudel/cart.html
cmd/holzcloud/templates/public/rudel/checkout.html
cmd/holzcloud/templates/public/rudel/order.html
cmd/holzcloud/templates/public/rudel/style.css
cmd/holzcloud/assets/VENDOR.md
  </files>

  <read_first>
cmd/holzcloud/templates/public/weide/{list,search,gate,404,maintenance,shop,product,cart,checkout,order}.html
— der Aufbau in der neuen Sprache. `weide` zeigt in diesen zehn Ansichten
jedes Feld, das die heutige `rudel`-Fassung zeigt, und drei mehr.
cmd/holzcloud/templates/public/rudel/{list,search,gate,404,maintenance,shop,product,cart,checkout,order}.html
— der heutige Stand; Ziel, Feldnamen und verstecktes Feld der Formulare stehen
hier und bleiben unveraendert.
cmd/holzcloud/templates/public/weide/style.css — Zeilen 1409..1760: Archiv,
Suche, Schranke, die zwei kurzen Seiten und der Laden.
cmd/holzcloud/assets/VENDOR.md — der Abschnitt `## Schriften` und das
Aktualisierungsrezept am Ende.
  </read_first>

  <action>
**Die zehn Ansichten (D-12).** Jede wird nach dem Muster der gleichnamigen
Ansicht in `weide/` neu geschrieben, also in derselben Bauteil-Sprache
(`.hc-wide`, `.hc-pane`, `.hc-btn`, `.hc-field`, `.hc-input`, `.hc-alert`,
`.hc-status`, `.hc-eyebrow`, `.hc-lede`, `.hc-dim`). Keine Ansicht darf
verschwinden: `internal/template/loader.go:587` kennt die Liste, und
`TestShippedThemesRenderEveryView` rendert jede mitgelieferte Vorlage
vollstaendig.
`weide` ist in diesen zehn Ansichten die Obermenge der heutigen
`rudel`-Fassung; drei Stellen kommen dadurch neu hinzu und werden uebernommen:
das Ziel nach dem Freischalten in `gate.html`, die Schlagwortliste am Fuss des
Archivs und der Bestellstatus in `order.html`. Alles, was die heutige Fassung
zeigt, bleibt: `.Meta.Message` in `404.html` (diese Ansicht ist auch die
Antwort auf einen abgelaufenen Vorschaulink — ohne die Meldung erfaehrt der
Empfaenger nie, dass er nur einen neuen Link braucht), `.Gate.Hint`, der
Rueckfallsatz mit `.Site.Name` in `maintenance.html`, der Titel aus
`.Page.Title` in `search.html`, die Zahl der Treffer und der Rueckweg vom
Schlagwort zum ganzen Archiv in `list.html`.
Ziel, Feldnamen und verstecktes Feld der Formulare bleiben unveraendert —
`/freischalten` mit `seite` und `passwort`, `.Checkout.Action`, die Namen der
Warenkorbfelder. Wer hier etwas umbenennt, macht die geschuetzte Seite oder die
Kasse unerreichbar.
`{{with}}` statt `{{if}}` ueberall dort, wo ein Zeiger fehlen kann.

**`style.css` — der Rest der CMS-Schicht.** Vor den Druck-Abschnitt anfuegen,
nach dem Vorbild der genannten Zeilen aus `weide`:
- Archiv: `.archive-count`, `.archive-list`, `.archive-entry` mit Bildspalte,
  `:has()` fuer den Eintrag ohne Bild, `.archive-pager`, `.archive-terms`,
  `.archive-empty`. Haarlinien zwischen den Eintraegen und keine Karten — eine
  Liste von Kaesten liest sich schlechter als eine Liste. Das Vorschaubild des
  Eintrags kommt als blosser URL ohne Angabe zur Bildmitte
  (`.Archive.Entries` traegt nur `ImageURL`), wird aber zugeschnitten; es
  bekommt deshalb hier — und nur hier, ausserhalb des Baustein-Abschnitts —
  eine feste Bildmitte im oberen Drittel, mit einem deutschen Kommentar dazu:
  auf einem Hundefoto steht der Kopf fast immer oben, und ein Zuschnitt aus der
  Mitte trifft den Rumpf.
- Suche: `.search-form`, `.search-count`, `.search-results`, `.search-result`.
- Schranke: `.gate-card` und ihre Teile, eine Flaeche in der Mitte statt einer
  Seite, weil es hier genau eine Sache zu tun gibt.
- Die zwei kurzen Seiten: `.page--kurz`.
- Der Laden: `.price` mit `font-variant-numeric: tabular-nums` (ohne eine
  Familie fester Breite ist das die Zeile, die Betraege untereinander
  ausrichtet), `.price-switch`, `.shop-categories`, `.product-grid`,
  `.product-card`, `.product`, `.product__gallery`, `.product__buy` (klebend,
  auf schmalen Fenstern statisch), `.cart-lines`, `.cart-line`,
  `.cart-summary`, `.cart-totals`, `.cart-actions`, `.checkout__grid`,
  `.checkout__group`, `.field-row`, `.choice` (die ganze Zeile ist das Ziel,
  nicht das Kaestchen), `.checkout__summary`, `.order__*`, samt den zwei
  Medienabfragen, die die Zweispalter auf schmalen Fenstern stapeln.
Der `@media print`-Abschnitt aus Aufgabe 1 bleibt die letzte Regel der Datei;
alles Neue geht davor.

**`VENDOR.md`.** Im Abschnitt `## Schriften` steht heute, dass die beiden
Manrope-Dateien an ZWEI Orten liegen. Es sind ab dieser Aufgabe drei: `rudel`
kommt dazu, byteweise identisch, mit denselben SHA-256-Werten. Die Begruendung
bleibt dieselbe und muss nicht neu erfunden werden — eine Vorlage muss als
Archiv fuer sich stehen, und `/t/fonts/…` zeigt immer in die Vorlage, die
gerade ausgeliefert wird. Im Aktualisierungsrezept den dritten Zielpfad
ergaenzen (die Variable, das `cp` und das `cmp`), damit ein kuenftiger Wechsel
nicht eine Kopie stehen laesst, und den Satz nachziehen, der die Stylesheets
aufzaehlt, in denen die Werte gepflegt werden. JetBrains Mono bleibt bei
`holzcloud` allein; auch `rudel` benutzt keine Schrift fester Breite.

**Zum Schluss** die vollstaendige Testreihe laufen lassen, nicht nur die zwei
Pakete: `go test ./...`. Ausserdem `go run ./tools/i18n` — diese Arbeit fuegt
keine uebersetzbaren Zeichenketten hinzu, aber der Lauf ist billig und die
Reihe ist ein stehendes Tor des Projekts.

**Kein Go-Code aendert sich (D-17).** `rudel` steht schon in
`BuiltinTemplates` (internal/template/loader.go:518) und schon in der
Themenliste von `TestShippedThemesRenderEveryView` (render_test.go:195); der
Datenvertrag wird nicht erweitert. Wer beim Uebersetzen der zehn Ansichten auf
ein Feld stoesst, das es nicht gibt, laesst die Stelle weg — er ergaenzt nicht
`internal/template/loader.go`. `git status` darf am Ende keine Datei unter
`internal/` und keine unter `cmd/holzcloud/*.go` zeigen.
  </action>

  <verify>
    <automated>
cd /Users/holz/Projects/holzcloud-cms
test "$(ls cmd/holzcloud/templates/public/rudel/*.html | wc -l | tr -d ' ')" = 13 && echo "12 Ansichten plus layout.html"
grep -q 'Meta.Message' cmd/holzcloud/templates/public/rudel/404.html && echo "abgelaufener Vorschaulink erklaert sich"
grep -q 'Gate.Path' cmd/holzcloud/templates/public/rudel/gate.html && echo "Ziel nach dem Freischalten steht da"
grep -q 'Site.Name' cmd/holzcloud/templates/public/rudel/maintenance.html && echo "Wartungssatz nennt den Betrieb"
grep -q 'rudel' cmd/holzcloud/assets/VENDOR.md && echo "der dritte Ort der Schriften ist verzeichnet"
test -z "$(git status --porcelain -- internal cmd/holzcloud/main.go)" && echo "kein Go-Code angefasst"
awk '/^@layer/{n++; last=NR} /^\/\* ══ CMS/{cms=NR} END{ if (n==4 && cms>0 && last<cms) print "Kaskade ok"; else { print "FEHLER"; exit 1 } }' cmd/holzcloud/templates/public/rudel/style.css
test "$(grep -c -E -- '--hc-(brand|paper|font|radius)[[:space:]]*:' cmd/holzcloud/templates/public/rudel/style.css)" = 0 && echo "Regler werden nur gelesen"
test "$(awk '/^\/\* ══ Die Bausteine/{f=1;next} /^\/\* ══ /{f=0} f' cmd/holzcloud/templates/public/rudel/style.css | perl -0777 -pe 's{/\*.*?\*/}{}gs' | grep -c 'object-position')" = 0 && echo "die Bildmitte aus dem Kern bleibt unangetastet"
go build ./...
go run ./cmd/holzcloud template check cmd/holzcloud/templates/public/rudel
go test ./internal/template/... ./internal/tmplmgr/...
go test ./...
go run ./tools/i18n
    </automated>
  </verify>

  <done>
Dreizehn HTML-Dateien liegen in der Vorlage, jede in der neuen Sprache. Kein
Feld, das die alte Fassung zeigte, ist verlorengegangen; drei Stellen sind aus
`weide` dazugekommen. `template check` meldet „no problems found",
`go test ./...` ist gruen, `go run ./tools/i18n` meldet 0 offen und 0 verwaist.
VENDOR.md verzeichnet den dritten Ort der beiden Schriftdateien samt Rezept.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Vorlage | Jede Ressource, die eine Seite dieser Vorlage anfordert, muss vom eigenen Ursprung kommen. Ein `<link>`, ein `url()` oder ein `src` auf einen fremden Host verraet jeden Besucher an einen Dritten und bricht die `Content-Security-Policy` in internal/web/headers.go. |
| Vorlagen-Archiv -> Server | Diese Vorlage ist zugleich ein gueltiges hochladbares Archiv. Was `internal/tmplmgr` beim Upload prueft, muss sie selbst bestehen. |
| `.Site.Design` -> `<style>` | Die Werte der Website landen in einem `<style>`-Block im Kopf. Sie werden in internal/design geprueft, bevor sie dorthin kommen. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-t0s-01 | Information Disclosure | `@font-face` in rudel/style.css | high | mitigate | Manrope wird aus `weide/fonts/` kopiert, nicht verlinkt; `src` zeigt auf `/t/fonts/…`. Geprueft von `CheckNoExternalRefs` ueber `template check` und von `TestBuiltinTemplatesHaveNoExternalRefs` (internal/tmplmgr/external_test.go). |
| T-t0s-02 | Tampering | die dreizehn HTML-Dateien | high | mitigate | Kein `.js`, kein `<script>` ausser `application/ld+json`, kein `on*`, keine `javascript:`-Adresse. Geprueft von `CheckNoScripts` (internal/tmplmgr/script.go) ueber `template check`. |
| T-t0s-03 | Tampering | die zwei kopierten woff2 | medium | mitigate | `cmp` gegen die Kopien in `weide`, die ihrerseits in VENDOR.md mit SHA-256 verzeichnet sind, in jeder der drei Aufgaben. Eine stillschweigend veraenderte Schriftdatei faellt beim ersten Lauf auf. |
| T-t0s-04 | Elevation of Privilege | `{{.Site.Design}}` im `<style>` | medium | accept | Unveraendert gegenueber allen acht mitgelieferten Vorlagen: `internal/design` laesst nur Hex-Farben, eine Schrift aus einer festen Liste, eine Zahl fuer das Mass und eine fuer die Rundung durch. Dieser Plan aendert dort nichts. |
| T-t0s-05 | Information Disclosure | Entwuerfe auf der oeffentlichen Seite | medium | accept | Die Vorlage fragt nichts ab; die Auswahl trifft `internal/public`. Dieser Plan aendert dort nichts. |
| T-t0s-06 | Information Disclosure | `.Meta.OGImage` als `src` im Aufmacher | low | accept | Der Wert wird in `withOGImage` aus der eigenen kanonischen Adresse gebaut und zeigt deshalb auf den eigenen Ursprung; dieselbe Adresse steht in jeder Vorlage bereits als `og:image` im Kopf. |
| T-t0s-07 | Tampering | Bildmitte je Kachelbild (`internal/block/render.go`) | low | mitigate | Die Vorlage setzt diese Eigenschaft im Baustein-Abschnitt nirgends selbst und behaelt den Zuschnitt; ein Prueftor liest den Abschnitt kommentarbereinigt und verlangt null Treffer. Ohne das waere die Angabe des Kerns tote Auszeichnung. |
| T-t0s-SC | Tampering | Paketinstallation | low | accept | Diese Arbeit ruft keinen Paketmanager auf — kein npm, kein pip, kein cargo, kein neues Go-Modul. Es gibt nichts zu auditieren. |
</threat_model>

<verification>
Nach der letzten Aufgabe, vom Projektwurzelverzeichnis aus:

1. `go run ./cmd/holzcloud template check cmd/holzcloud/templates/public/rudel`
   meldet „no problems found". Das ist das Tor, auf das es ankommt: es rendert
   alle zwoelf Ansichten zweimal — einmal gegen eine voll besetzte Seite und
   einmal gegen eine, bei der jedes optionale Feld leer ist. Der zweite Lauf
   ist der, der einen ungesicherten Zeiger findet.
2. `go test ./internal/template/... ./internal/tmplmgr/...` ist gruen.
3. `go build ./...` ist gruen.
4. `go test ./...` ist gruen.
5. `go run ./tools/i18n` meldet 0 offen, 0 verwaist.
6. Die Kaskade: genau vier `@layer` am Zeilenanfang, alle vor der Markierung
   der CMS-Schicht.
7. Die Vorlage setzt keinen der vier gebrueckten Reglernamen.
8. Die zwei woff2 sind byteweise identisch mit den Kopien in `weide`.
9. Das Menue steht unter der Schwelle als Spalte, der Sprunglink ist erstes
   Kind von `<body>`.
10. Der Baustein-Abschnitt setzt die Bildmitte nirgends selbst.

**Nicht automatisiert, und darum wert, hinterher zu tun:** ein Blick im Browser
auf Startseite, Textseite, Archiv, eine Galerie mit hochformatigen Bildern und
ein schmales Fenster mit sechs Menuepunkten. Genau dieser Blick fand bei
`holzcloud` und bei `weide` je einen Fehler, den keine der obigen Pruefungen
sah. Dafuer gibt es `/gsd-verify-work` gegen einen laufenden Server.
</verification>

<success_criteria>
- Die Vorlage `rudel` steht auf gebrochenem Weiss mit Waldgruen und Manrope;
  keine System-Serifen, kein Cremegrund, keine schmale Spalte.
- Die zwei Befunde aus der Sichtpruefung von `weide` stehen im ersten Wurf
  drin: das Menue ist unter der Schwelle eine Spalte hinter einem Umschalter,
  und ein Bild allein in seinem Absatz spannt breit und ist in der Hoehe
  begrenzt.
- Galerie und Kartenreihe vertragen Hochformat, und die Bildmitte, die der Kern
  je Kachelbild mitgibt, wirkt weiterhin.
- Alle zwoelf Ansichten sind erhalten und uebersetzt; keine ist verschwunden.
- Alle neun Bausteinarten sind ueber die Bruecke gekleidet.
- Die drei Stufen der Kaskade stehen in dieser Ordnung: Schichten, CMS-Schicht,
  `.Site.Design`.
- Die schwaechste Tinte erfuellt AA fuer die kleinste Schriftgroesse — mit der
  auf diesem Papier gemessenen Zahl, und die Rechnung steht im Kommentar.
- Eine eingestellte Rundung zieht die ganze Radienfamilie mit.
- Kein JavaScript, keine externe Ressource, keine Aenderung an Go-Code, keine
  Erweiterung des Datenvertrags.
- Jede Datei traegt deutsche Kommentare, die das WARUM erklaeren.
</success_criteria>

<output>
Create `.planning/quick/260903-t0s-vorlage-rudel-neu-gestalten-gruene-schwe/260903-t0s-SUMMARY.md` when done
</output>
